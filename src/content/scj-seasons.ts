/**
 * 신천지 절기 — 달력 표시용 **자리** (2026-08-13 리드 지시).
 *
 * ⚠️ **원문 파일 대기 중이라 비어 있다.** 절기 이름·날짜가 오면 여기만 채우면
 * 달력(`WeeklyPlanPage`)에 바로 뜬다. 교리 관련 내용이라 **임의로 지어 넣지 않는다**
 * (불변식 5 — 원문 그대로, 재작성 금지).
 *
 * 형식: `"YYYY-MM-DD": "절기 이름"` — 공휴일(`holidays-kr-2026.ts`)과 같은 꼴이다.
 */
export const SCJ_SEASONS: Record<string, string> = {};
