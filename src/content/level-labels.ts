/**
 * 과정 단계의 **표기와 색** — 이 파일 하나가 정본이다 (2026-08-21).
 *
 * ⚠️ **일부러 가볍게 둔 파일이다.** 사이드바(`shell/nav.ts`)가 이것을 읽는데, 셸에 매달린
 * 것은 **모든 화면이 함께 진다**(2026-08-18에 배웠다). 그래서 목업·원문을 안고 있는
 * `curriculum-mock.ts`에서 갈라냈다 — 여기에는 문자열 네 표만 둔다.
 * `curriculum-mock`은 이 파일을 그대로 다시 내보내므로 종전 import 자리는 그대로 산다.
 */

/**
 * 과정 단계 — `student-profiles`의 `CourseLevel`과 같은 어휘를 쓴다.
 *
 * ⚠️ **이 값은 데이터다. 화면 표기가 아니다.** 저장된 수강생·자료·폴더가 이 문자열을
 * 그대로 갖고 있어 바꾸면 후방 마이그레이션이 된다(불변식 10).
 * 화면에 낼 때는 아래 `LEVEL_NAME`을 거친다 — **단계 이름을 직접 찍지 않는다.**
 */
export type LessonLevel = "초등" | "중등" | "고등";

/**
 * 화면에 내는 과정 이름 — **주제 이름으로 부른다** (2026-08-21 리드 확정).
 *
 * ⛔ **「초등·중등·고등」을 화면에 쓰지 않는다 — 학원법 때문이다.** 학년 편성처럼 읽히는
 * 표기를 없애라는 지시이고, 연속 번호 금지(2026-08-15)의 연장이다.
 * 과정을 부를 때는 **그 과정이 다루는 주제**로 부르고, 색(연두·주황·파랑 계열 바탕)이
 * 곁에서 거든다.
 *
 * ⚠️ **이름은 리드가 정해 준 그대로다** — 같은 날 두 번 고쳐 여기 이력이 있다:
 * 색 이름(연두·주황·파랑)으로 냈다가 「고르는 자리에 연두 주황 파랑도 빼줘」 →
 * 「연두 대신 천국 비유 풀이 / 주황 대신 성경 예언과 성취 / 파랑 대신 계시록으로 정정」.
 * 다듬거나 줄이지 않는다 — 고칠 일이 있으면 리드에게 확인받는다.
 * ⚠️ 자료 원문 제목(「초등 과정 표준 강의안…」)과 용어집 정의문은 **원문이라 손대지 않는다**
 * (불변식 5). 사이트가 짓는 문구에만 이 표를 쓴다.
 */
export const LEVEL_NAME: Record<LessonLevel, string> = {
  초등: "천국 비유 풀이",
  중등: "성경 예언과 성취",
  고등: "계시록",
};

/*
  ⚠️ **좁은 칸용 한 글자 표기(`LEVEL_SHORT`)는 2026-08-21에 없앴다.**
  종전 「초·중·고」를 색 이름 첫 글자(「연·주·파」)로 바꿨더니 리드가 짚었다 —
  「연두색 배경 안에 그릇~지팡이 이런 식으로 단어가 들어가는 거지 색깔 이름이 아니다」.
  좁은 칸에는 **과수 제목**이 들어가고 단계는 바탕색이 알린다(`shortLessonLabel`).
*/

/**
 * 단계 색 — **연두 · 주황 · 파랑 계열** (2026-08-21 리드 지시).
 * 종전 「초록 · 주황 · 남색」(2026-08-15·18)을 대체한다.
 * 색 이름은 화면 글자로 내지 않는다 — 이름은 위 `LEVEL_NAME`(주제 이름)이 맡고 색은 거든다.
 *
 * ⚠️ **색값은 여기 없다.** `index.css`의 `@theme`에 토큰(`--color-level-*`)으로 있고
 * 여기는 그 유틸리티 이름만 짝지어 둔다 — 화면에 색을 하드코딩하지 않는다는 규칙 그대로다.
 * 제목 표시는 「옅은 바탕 + 진한 글자」 한 벌이라 둘을 함께 적는다.
 */
export const LEVEL_TONE: Record<LessonLevel, string> = {
  초등: "bg-level-el-soft text-level-el",
  중등: "bg-level-mid-soft text-level-mid",
  고등: "bg-level-high-soft text-level-high",
};

/** 색만 필요한 자리(글자·테두리)용 */
export const LEVEL_TEXT: Record<LessonLevel, string> = {
  초등: "text-level-el",
  중등: "text-level-mid",
  고등: "text-level-high",
};
