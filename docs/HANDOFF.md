# HANDOFF — 새 세션은 여기부터

**갱신 2026-08-13.** `main`은 깨끗하고 전부 푸시돼 있다.
**원격 브랜치도 전부 main에 들어갔다** — 지금 미머지 작업은 없다.

> 이 문서는 **지금 상태와 다음 할 일**만 담는다. 날짜별 상세 기록은
> [HISTORY.md](HISTORY.md)로 옮겼다 — "왜 이렇게 됐나"가 궁금할 때만 그쪽을 본다.
> 작업 절차와 함정은 `zion-ark-web` 스킬에 있다 (세션 시작 시 자동 적용).

## 30초 오리엔테이션

| 항목 | 값 |
| --- | --- |
| 이 저장소 | **웹 프로토타입** (React 19 + Vite 6 + TS strict + Tailwind 4, `src/**`) |
| GitHub | https://github.com/qtg002505/zion-ark (public) |
| 팀 공유 링크 | https://qtg002505.github.io/zion-ark/ — `main`에 push하면 2~3분 뒤 자동 갱신 |
| 리드 확인용 링크 | https://claude.ai/code/artifact/97c2d451-7903-4c8f-8ede-09b5d72c7d25 (화면만 · 음악 안 나옴) |
| 개발 셸 | Windows PowerShell (`npm.cmd` · `npx.cmd`), 미리보기는 `.claude/launch.json`의 `zion-ark-dev` |
| 검증 | `npx.cmd tsc -b --noEmit` → `npm.cmd run build` → **새 탭**에서 콘솔 0 · 375px 넘침 0 |

⚠️ **원 운영 저장소와 헷갈리지 말 것.** 2026-08-08 지시문은 원 저장소(vinext + D1/R2)
기준으로 쓰였다 — R2·D1·API·마이그레이션은 여기 몫이 아니다. 이 프로토타입이 맡는 것은
**화면 · 동선 · 권한 구조**다.

## 지금까지 만든 것 (화면 기준)

| 대주제 | 상태 |
| --- | --- |
| 홈 · 전체 현황 | ✅ 요약 + **등급별 명단**(A~E · 오픈 노란 줄) |
| 기수 현황 | ✅ 5탭. 출석은 **격자 표기**, **보강 포함 출석률** |
| 기수 주간계획 | ✅ **월별 달력** · 중요 일정란 · 날짜 팝오버 · **년·월 바둑판 선택** · **엑셀 진도표 업로드** |
| 강의 도우미 | 🔶 초등 23강·고등 원문 + **강별 PPT·영상 링크**. **밭갈이 4파트·예배설교가 도우미 안에서 자료를 연다**(`/teaching`). 중등 원본 대기 · 4항목 구조 미착수 |
| 분반 · 보강 도우미 | 🔶 폴더·링크 자리만. **보강 자료 7개 폴더가 비어 있다** |
| 수강생 관리 도우미 | ✅ **목록 1단**(표 11열) → 줄을 누르면 **팝업**. 반응 붙여넣기 자동 분류. **수강생 성향 분석**(`/tendency`) — 동의·비식별화 게이트 동작, AI 응답은 백엔드 대기 |
| 상담 도우미 | ✅ 테마 12종 UGC(도움됨 1인 1회·신고·숨김) · 성향 가이드 · **스토킹처벌법 주의** |
| 자료실 | ✅ **구획 폐지 — 폴더 한 줄기**. 아카이브 2폴더 · 말씀광장 · 천지일보. 파일 원본은 R2 대기 |
| 공지 · 어록 | ✅ 독립 대주제 |
| 사명자 심방 도우미 | 🔶 프롬프트 생성기만. **번아웃 척도 원본 대기** |
| 12지파 선교센터 | ✅ **지도 동작 확인** (카카오맵 키 등록 완료). 금색 핀 · 확대하면 층·기수 · 눌러서 **진도**. 대전(맛디아) 2곳 |
| 마이페이지 | ✅ **개인 주간 스케줄러**(담당 수강생 생일 자동) · 즐겨찾기 · 열람 기록 · **화면 밝기** |
| 셸 | ✅ 네이비 테마 · **다크 모드**(헤더 단추 · 기기 설정 따르기) · **화면 전환 효과** · **NEW 뱃지** · **오늘의 어록** · **배경 음악**(S-POP 6곡) · 맨 위로 |
| 검색(Ask AI) | ✅ **어절 단위 · 조사/질문어 처리 · 점수순**. 용어집까지 훑는다. AI 응답은 백엔드 대기 |

## 다음에 할 수 있는 것

1. **강의 도우미 4항목** (지시문 4단계) — 재료가 다 모였다. 초·중·고 정의와 「인교섬 미션」
   정의가 `src/content/glossary.ts`에 있고, **인교섬은 계정이 없으므로** 이 화면은
   "강사·전도사가 인교섬에게 무엇을 부탁할지" 보는 쪽으로 만든다
2. **보강 자료 7개 폴더 채우기** — 원문을 받으면 바로 넣는다
3. **성향 분석에 실제 AI를 붙이기** — 화면·게이트·프롬프트는 다 됐다(`/tendency`).
   남은 것은 **우리 서버를 거치는 호출**(`VITE_ASK_API_PATH`)과 **서버 쪽 재검사**다.
   ⚠️ 브라우저의 `redactForAI`만 믿으면 안 된다 — 사람이 쓴 이름은 정규식이 못 잡는다.
   동의 스위치는 **시범 장치**이니 실제 동의 데이터가 붙으면 걷어낸다
4. **분반·보강도 제 카테고리 안에서 열지 물어보기** — 강의 도우미는 2026-08-13에 안으로
   들어왔는데(`/teaching`) 보강 자료·보강 콘텐츠는 아직 자료실 화면으로 나간다.
   부품(`FolderLibrary`)은 이미 공용이라 **화면 하나만 더 만들면 끝난다** — 리드 확인 후 결정
5. **상담 도우미에 소주제 미루기를 켤 수 있게 됐다** — 종전에는 소주제 안의 등록 모달이
   `contain: paint`에 갇혀 못 켰는데, 2026-08-13에 모달을 `Portal`로 빼서 그 제약이 풀렸다.
   `Counseling.tsx`의 `<Accordion>`에 `deferOffscreen`을 주고 **모달이 화면 가운데 뜨는지**
   확인하면 된다 (본문 18,000px짜리 화면이다)
6. **팀원 검수 응답 받기** — 이슈 #2(파트 B)는 넷 다 반영해 올렸다. **#1·#3·#4·#5는
   아직 응답이 없다**(파트 A·C·D·E). #3은 세팅 완료만 올라와 있다

## 사람이 주지 않으면 못 하는 것

- **원본**: 중등 교안 · 번아웃 척도 문항 · 사명자 회복용 성향 원문 · 성향 상담 내부 원문 ·
  강별 PPT/영상 실제 주소 · **아카이브 파일(실상 뮤지컬 영상 · 계시록 삽화 —
  내려받기 링크가 붙어야 열린다)** ·
  **나머지 11개 지파 선교센터 주소** (대전 두 곳은 2026-08-11에 **맛디아 지파**로 확정)
- **주소**: 상담 GPT(커스텀 GPT) 링크 — 상담 도우미 ⑫와 사명자 심방 양쪽에서 대기
- **정책**: 마이페이지 열람 기록 보관 기간(권장 90일) · 상담법 신고 처리 기준 ·
  수강생 화면 열람 기록을 켤지 ·
  ⚠️ **외부 AI 동의를 어떻게 받을지** — 수강 신청서에 넣을지 기수 단위로 받을지가 미정이다.
  코드는 `ConsentState`(`granted`/`denied`/`unknown`) 자리를 만들어 뒀고 **`unknown`이면
  보내지 않는다.** 실제 동의 데이터가 붙기 전까지 외부 AI로는 아무것도 나가지 않는다
- **확인**: `OPEN_QUESTIONS.md` §C의 C-1(신앙전환↔영적전환) · C-2(주전도사 역할 코드) ·
  C-3(인도자·섬김이 구분) · C-5(회 구분 도입) · C-7(마팔 연동) · §E(텔레그램 발송)

⚠️ **용어 정의는 전부 받았다** (2026-08-08). 원문은 `src/content/glossary.ts`,
연결 관계는 `docs/decisions/GLOSSARY.md`. 정의가 왔어도 **코드 값으로 굳히지 않는다**.

## 지금 구조에서 알아야 할 것

- **자료실 구획이 없어졌다** (2026-08-13). 「강사 도우미 자료실 / 외부 자료실」 두 갈래가
  사라지고 **폴더 한 줄기**가 됐다 — 「외부 자료실」은 애초에 없던 개념이고, 외부 매체는
  자료에 붙는 참고 링크였다.
  ⚠️ **`section` 필드는 지우지 않았다.** 저장된 자료가 값을 갖고 있어 지우면 후방
  마이그레이션이다 — **읽는 곳이 없을 뿐 남아 있다**(`division`과 같은 취급)
  - 자료 목록·상세·등록은 **`src/components/FolderLibrary.tsx` 한 벌**을 자료실과 강의
    도우미가 나눠 쓴다. 새 화면에서 폴더 자료를 열어야 하면 **이걸 props로 부른다**
  - 옛 링크(`?section=…`·`?tab=…`)는 그대로 동작한다 — 북마크가 죽지 않게 계속 받는다
- **성향 분석의 게이트는 실제로 막는다** (`/tendency`, 2026-08-13).
  동의가 「모름」이면 **프롬프트 자체를 열지 않는다** — 사람이 복사해 붙이는 것도 외부로
  보내는 것이기 때문이다. 관찰 글은 `redactForAI`를 거친 것만 프롬프트에 실리고,
  무엇을 가렸는지 화면에 보여 준다.
  ⚠️ 화면의 **동의 스위치는 시범 장치**다 — 실데이터 투입 전에 없앤다(시범 로그인과 같다)
- **다크 모드는 화면 파일을 한 곳도 안 고치고 넣었다** (2026-08-11).
  `src/index.css`의 `[data-theme="dark"]`가 팔레트 변수를 갈아 끼운다.
  ⚠️ **진한 배경으로 쓸 수 있는 클래스가 정해져 있다** — `CLAUDE.md` 화면 규칙의 목록을 본다.
  목록 밖 단계를 배경에 쓰면 다크에서 하얗게 뜬다
- **밝은·어두운 두 화면 모두 AA(4.5)를 통과한다** (2026-08-11 실측 — 22개 라우트 + 팝업·드로어).
  ⚠️ `text-zion-300`은 **글자일 때만** 따로 어두운 값(`--text-zion-muted`)으로 바뀐다 —
  같은 단계를 `border-zion-300`·`bg-zion-300`으로 쓰면 종전대로 옅다. 일부러 그렇게 뒀다
  (비활성 버튼은 흐릿해야 눌리지 않는 것으로 읽힌다)
- 🔓 **외부 AI를 쓸 수 있게 됐다** (2026-08-11 리드 변경). 종전 「수강생 개인정보를 AI
  입력에 넣지 않음」이 **「동의받고 비식별화한 정보만 보낸다」**로 바뀌었다 (불변식 4).
  텔레그램 봇 연동이 이 한 줄에 막혀 있었다.
  **보내는 쪽은 `src/lib/privacy.ts`의 `prepareForAI(글, 동의상태)`만 쓰면 된다** —
  동의 확인과 비식별화가 그 안에서 끝난다. 동의를 모르면 함수가 막는다(기본값이 「안 보냄」).
  - ⚠️ **정규식은 사람 이름을 못 잡는다.** 「김민수 형제님」은 걸리지만 「민수가 요즘」은
    안 걸린다. 그래서 **서버에서 같은 검사를 다시** 해야 하고, 브라우저 검사만 믿으면 안 된다
  - ⚠️ **키를 브라우저에 두지 않는다** — 우리 서버를 거쳐 부른다 (`VITE_ASK_API_PATH`)
  - 반응 분류(`reaction-classify.ts`)의 낱말 규칙은 **폴백으로 남긴다** — 동의가 없거나
    API가 죽어도 화면이 멈추면 안 된다
- **카카오맵 키는 `.env`에 들어 있다** (앱 「시온 아크」 ID 1540603, 2026-08-11 등록).
  `.env`는 git에 안 올라가므로 **새 기기에서 받으면 지도가 안 뜬다** — 콘솔에서 키를 복사해
  `VITE_KAKAO_MAP_KEY`에 넣는다. 등록된 도메인은 `qtg002505.github.io` ·
  `localhost:5173` · `localhost:4173`이다.
  ⚠️ **개발 서버가 다른 포트로 뜨면 그 포트도 콘솔에 더해야 한다** (autoPort로 바뀔 수 있다).
  ⚠️ **제품 설정 > 카카오맵을 켜야 한다** — 키와 도메인이 맞아도 이게 꺼져 있으면 **503**이 온다
- ⚠️ **화면 위에 띄우는 것은 `src/components/Portal`로 감싼다** (2026-08-13) —
  모달 · 팝오버 · 시트 전부. `fixed inset-0`에 `z-50`을 줘도 **`main` 안에 있으면 헤더에 눌린다**
  (`main`이 `view-transition-name` 때문에 쌓임 맥락을 만든다). 실제로 수강생 상세 팝업 머리가
  잘렸다. **숫자를 올려도 안 고쳐진다** — 맥락 밖으로 꺼내야 한다.
  지금 열 곳이 이미 `Portal`을 쓴다. **새 모달도 처음부터 감싼다**
- **링크는 `src/components/TransitionLink`에서 가져온다** (2026-08-11).
  `react-router-dom`에서 직접 가져오면 화면 전환 효과가 조용히 빠진다 —
  라우터의 `viewTransition`은 데이터 라우터 전용인데 이 앱은 `<BrowserRouter>`다
- **데이터는 브라우저 안에만 있다** (localStorage). 같은 브라우저에서 재로그인하면
  그대로 남지만 **다른 기기·브라우저에서는 안 보인다** — 백엔드가 붙어야 풀린다
- **음악은 팀 공유 링크(Pages)에서만 들린다.** 아티팩트 프리뷰는 단일 HTML이라 음원을
  넣으면 16MB 제한을 넘어 일부러 뺐다
- **시범 로그인은 인증이 아니다.** 브라우저 콘솔에서 `localStorage`를 고치면 아무 역할이나
  될 수 있다 — 실데이터 투입 전 정리가 선행이다
- **번들이 2.9MB(gzip 870KB)다.** 현장에서 휴대전화로 여는 사이트라 라우트별 코드 분할을
  검토할 때가 됐다. 빌드가 매번 경고를 낸다
- **자동 테스트가 0건이다.** 도움됨 중복 클릭·타인 글 수정 차단 같은 것을 세션마다 손으로
  확인하고 있다 — 다음 사람이 회귀를 못 잡는다

## 팀 운영

- 파트 분담은 `TEAMWORK.md`. 팀원은 `/work`(이슈 작업)와 `/push`(올리기) 스킬을 쓴다
- ⚠️ **리드는 `main`에 직접 push할 수 있다**(보호 규칙 bypass). 팀원 PR과 겹치지 않게
  파트 파일을 건드릴 때는 담당자에게 알린다
- ⚠️ **승인 필수는 꺼져 있다** — 파트 파일만 고친 PR은 팀원이 직접 머지한다.
  전부 리드가 보려면 GitHub → Settings → Branches → `main` → "Require approvals: 1"

### 브랜치를 합칠 때 — 2026-08-13에 배운 것

- ⚠️ **팀원에게 「최신 main에서 브랜치를 새로 따라」고 먼저 알린다.** 파트 B 브랜치가
  나흘 묵는 사이 main이 **73개 파일 8,888줄** 바뀌어, 단순 충돌이 아니라 **설계 방향이
  반대**가 됐다(상세를 팝업으로 여느냐 페이지로 여느냐). 오래 묵을수록 합치는 비용이
  기하급수로 는다
- ⚠️ **합칠지 물을 때는 작업 요약의 항목을 하나씩 짚는다.** 「팝업이냐 페이지냐」 한 축으로만
  물었더니 같은 브랜치의 다른 개선(**목록 1단화**)이 조용히 사라졌고, 리드가 화면을 보고서야
  발견했다. 축 하나로 묶어 물으면 나머지가 묻힌다
- **팀원 코드는 대비를 안 재고 올 수 있다.** 파트 B의 오행 뱃지가 밝은 면에 흰 글자라
  대비 2.13이었다. 머지 뒤 **밝은·어두운 두 화면에서 다시 잰다**
