# HANDOFF — 새 세션은 여기부터

**갱신 2026-08-21.** `main`은 깨끗하고 전부 푸시돼 있다.
**원격 브랜치도 전부 main에 들어갔다** — 지금 미머지 작업은 없다
(2026-08-18에 원격 7개를 `--no-merged`로 다시 확인했다. part-b 둘 · part-c 넷 전부 ahead 0).

> 이 문서는 **지금 상태와 다음 할 일**만 담는다. 날짜별 상세 기록은
> [HISTORY.md](HISTORY.md)로 옮겼다 — "왜 이렇게 됐나"가 궁금할 때만 그쪽을 본다.
> 작업 절차와 함정은 `zion-ark-web` 스킬에 있다 (세션 시작 시 자동 적용).

## 🔴 다음 세션이 먼저 볼 것 (2026-08-22 마감 시점)

받은 리드 지시는 **전부 반영해 푸시했다** — 마지막 커밋 `b071d00`. 지금 미반영 지시는 없다.
2026-08-22의 큰 줄기 셋 (상세는 `HISTORY.md` 「2026-08-22 (5)~(7)」):

1. **수강생 분류 대시보드** (`/overview` 최상단 · `OverviewClassify.tsx`) — 리드 시안 그대로.
   등급별 분류표(분반색 칩·열머리) + 상태 묶음판 10종(`content/student-states.ts` 정본) +
   (i) 산출 기준 호버 팝업. **이름을 끌어** 등급·분반·상태로 옮기고(분반 이동은 확인창),
   수강생 현황·상세 팝업의 체크 패널과 **양방향 동기화**된다(`studentStateMarks` ·
   `override.division`). 기수 일정 카드는 접힘 기본
2. **⛔ Center Flow(텔레그램 백엔드) 통합은 전량 취소됐다** — 리드가 저쪽 웹을 보고
   「프론트엔드가 다 깨졌다」. 원격 브랜치·로컬 사본까지 지웠고 scj는 무변경이었다.
   분석·매핑표는 `docs/decisions/CENTER_FLOW_INTEGRATION.md`에 판단 자료로만 남는다 —
   **재개도 리드 지시가 선행이다**
3. 2026-08-21분: 학원법 표기(아래 ⛔), 기수 세팅 아이콘, 메모 기록, 긴 자료 내부 스크롤

**⛔ 화면에 「초등·중등·고등」을 쓰지 않는다 — 학원법**
2026-08-21 리드 지시로 단계 이름을 화면에서 전부 뺐다. 진도 칸은 **바탕색 + 과수 제목**,
과정을 이름으로 부를 때는 **주제 이름** — 「천국 비유 풀이 · 성경 예언과 성취 · 계시록」
(같은 날 두 번 정정: 색 이름(연두·주황·파랑)으로 냈던 것을 주제 이름으로 바꿨다.
**색 이름도 화면 글자로 쓰지 않는다**). 저장값과 원문은 그대로다 — 규칙은 `CLAUDE.md`.
기수 세팅은 대주제에서 빠져 **사이드바 홈 행 옆의 작은 아이콘**이 됐다(리드 지시).
검색창 예시도 「믿음, 기도」로 바꿨다(리드 지시).

**리드 답을 기다리는 것**
- **Center Flow(텔레그램 백엔드) 통합 — ⛔ 2026-08-22 리드 지시로 전량 취소.**
  분석 후 UI 이식(토큰·메뉴·라이트 전환·모의 하네스)까지 갔으나, 리드가 저쪽 웹을 보고
  「지금까지 만든 대시보드랑 다르게 프론트엔드가 다 깨졌다」며 취소시켰다 — 저쪽
  `apps/web`은 우리 프로토타입이 아니라 **별개 화면**이라 팔레트만으로는 우리처럼 안 보인다.
  원격 브랜치 삭제·로컬 사본 삭제·launch 항목 제거까지 되돌렸고 **scj는 원래 무변경**이다.
  분석·매핑표는 `docs/decisions/CENTER_FLOW_INTEGRATION.md`에 판단 자료로 남겼다
  (§G 7건 포함 — **재개도 리드 지시가 있어야 한다**)
- **나이의 AI 반출 여부** — 리드가 종합 분석 입력으로 나이를 꼽았는데(회의록 6번)
  확정 반출 목록은 「나이 제외」다. 지금은 교제 구분(청년회·장년회)으로 대신한다
- **보안·데이터 관리 설계** (회의록 9번) — 기수 종료 후 보관·이관·열람 권한, 외부 AI
  승인 절차. 전부 `docs/decisions/OPEN_QUESTIONS.md` §F에 정리해 뒀다
- **누가 기수 세팅을 하는가** — 지금은 기수 기록 권한(해당 기수 강사·전도사)을 그대로 쓴다.
  사명자 배정처럼 관리직 몫으로 보이는 것이 섞여 있다
- 지파별·교회별 **체크리스트 적용 범위** (총회·지파 신학부장 논의 중) — 지금은 기수 단위
- **우수 기수 공개 범위** — 지금은 같은 기수 담당자에게만 보인다
- 사명자 현황의 **「눌렀을 때 단계별로 상세히」** — 리드가 취소한 「단계표 달성률
  전도사별 집계」와 겹칠 소지가 있어 **명단까지만** 만들어 뒀다. 뜻을 확인하고 이어간다

**리드가 취소한 것 (다시 만들지 않는다)**
- 「난해 질문 파트」 (강의자료의 난해한 반응 추가)
- **전도사별 달성률·우수/미흡 순위 공개** — 실무자 부담이 크다는 의견으로 뺐다.
  필요하면 **관리자 전용**으로 다시 검토한다 (2026-08-21 재확인 — 애초에 만들지 않았다)
- 「수강생 × 항목 한눈에」 격자 — 만들었다가 같은 날 뺐다(커밋 `445bd1b`에 있다)
- 「총회 점검(1주차·4주차)」 카드 — 퍼널에 이미 있어 뺐다(커밋 `2fec452`에 있다)

**사람이 줘야 이어갈 수 있는 것**
- 강의 도우미 4항목의 **나머지 세 칸 원문** (진도에 따른 질문 · 무신앙/신앙인 예상 질문).
  ⚠️ 항목 이름 자체가 2차 기록에서 온 것이라 **원문을 받을 때 이름도 함께 확인**한다
- 2026-08-18에 만든 **콘텐츠 네 폴더의 자료** (반증자료 · 교리비교 영상 · 신천지 오픈 3 ·
  우수 강의자 영상) — 자리는 있고 비어 있다. 2026-08-21 리드 지시의 「반증 자료·교리 비교
  영상·오픈 자료를 강의 도우미에 추가」가 이 폴더들이다 — **자리는 이미 있고 원문만 없다**
- **MBTI 신학부 원문** — 지금은 바깥 자료를 정리해 출처·한계와 함께 싣고 있다.
  원문이 오면 `src/content/mbti-guides.ts`를 갈아 끼운다

## 30초 오리엔테이션

| 항목 | 값 |
| --- | --- |
| 이 저장소 | **웹 프로토타입** (React 19 + Vite 6 + TS strict + Tailwind 4, `src/**`) |
| 기수 일정 | **8개월 · 월화목 수업** (2026-08-13 리드 확정) — 개강 3/2 · 종강 예정 10/29 · 수업 105회 · 35주 |
| GitHub | https://github.com/qtg002505/zion-ark (public) |
| 팀 공유 링크 | https://qtg002505.github.io/zion-ark/ — `main`에 push하면 2~3분 뒤 자동 갱신 |
| 리드 확인용 링크 | https://claude.ai/code/artifact/97c2d451-7903-4c8f-8ede-09b5d72c7d25 (화면만 · 음악 안 나옴) |
| 개발 셸 | Windows PowerShell (`npm.cmd` · `npx.cmd`), 미리보기는 `.claude/launch.json`의 `zion-ark-dev` |
| 검증 | `npx.cmd tsc -b --noEmit` → `npm.cmd test` → `npm.cmd run build` → **새 탭**에서 콘솔 0 · 375px 넘침 0 |

⚠️ **원 운영 저장소와 헷갈리지 말 것.** 2026-08-08 지시문은 원 저장소(vinext + D1/R2)
기준으로 쓰였다 — R2·D1·API·마이그레이션은 여기 몫이 아니다. 이 프로토타입이 맡는 것은
**화면 · 동선 · 권한 구조**다.

## 지금까지 만든 것 (화면 기준)

| 대주제 | 상태 |
| --- | --- |
| **메인 (`/`)** | ✅ **홈페이지 노릇 — 카테고리가 주인공** (2026-08-13 신설). 로고를 누르면 여기로. 기수 진행률 머리 · 고정 공지 · 역할별 다음 자리 안내. **게시판은 격자 밖 하단 전폭의 얇은 흰 막대**(2026-08-21 리드 지시 — 글자는 다크에서 안 뒤집히는 `zion-950`만) |
| 전체 현황 (`/overview`) | ✅ **수강생 분류 대시보드가 최상단** (2026-08-22 리드 지시 — 운영 스프레드시트 「1페이지」+리드 시안 반영): 담당·진도 카드 + 흰 KPI 6타일 + **(i) 산출 기준 팝업**(마우스 올리면) · **등급별 분류표**(등급 칸에 명수·비율, 열머리는 담당자/분반) · **상태 묶음판 10종**(병리적 우울·행함부담·이성교제자 등 — 원문 문자열, enum 아님). **이름을 끌어 등급·분반·상태로 이동**(수동 변경 기록 재사용 · 터치는 추가 단추). 종전 남색 5타일은 KPI 줄이 대체. **기수 일정은 접힘**(머리에 총 기간 요약 유지) · 일정 편집은 그대로 |
| 기수 현황 | ✅ 5탭 + **퍼널 지표 10종(접기)** · **사명자 현황** · **8개월 꺾은선 3선**. **출석 현황은 2026-08-15에 갈아엎었다**(아래 항목) · **기수 비교는 「같은 회차에서 견주기」**로 축이 바뀌었다. 기수 요약의 「출석률 분포」 히스토그램은 2026-08-14 CHG-01(담당자 확정)로 뺐다 |
| 월간·주간 계획 | ✅ **월간/주간 전환** · 공휴일 · 수강생 생일(강사·전도사만) · 절기 자리 · **일정 날짜 옮기기** · 중요 일정란 · 년·월 바둑판 · 엑셀 진도표 업로드. **2026-08-21에 기수 고르기가 붙었다** — 지난 기수 달력을 보고 진도표를 내려받는다(종료 기수는 읽기 전용) |
| **기수 세팅 (`/cohort-setup`)** | ✅ **2026-08-21 신설**. 양식 넷(진도표·단계 향상표·사명자·수강생) 내려받기/올리기 · 지파 보충 체크 항목(수강생 상세·「지금 우리 기수는」에 연동) · 편성 변동(전도사 변경·유급 이동·합반) · 지난 기수 조회. ⛔ 사명자·수강생 명단은 올려도 **저장하지 않는다**(읽어서 보여 주기만) |
| 강의 도우미 | 🔶 초등 23강·고등 원문 + **강별 PPT·영상 링크**. **밭갈이 4파트·예배설교가 도우미 안에서 자료를 연다**(`/teaching`). 초·중·고마다 **우수 교안·특강 / 우수 판서 / 흐름교육** 셋. **4항목 중 첫 칸 「시기 따른 관리 방향」이 들어갔다**(2026-08-18 · 접이식) — 나머지 세 칸은 원문 대기. 중등 원본 대기. ~~강의 녹취 정리~~는 2026-08-15에 없앴다 |
| 분반 · 보강 도우미 | 🔶 **자료를 제 카테고리 안에서 연다**(`/makeup`, 2026-08-18 — 종전에는 자료실로 나갔다). **보강 자료 7개 폴더가 비어 있다** |
| 수강생 관리 도우미 | ✅ **목록 1단**(표 11열) → 줄을 누르면 **팝업**. 반응 붙여넣기 자동 분류. **수강생 성향 분석**(`/tendency`) — 수강생을 고르면 기록된 에니어그램 유형의 원문(성장과정·단계향상·관리팁·보강 성구)을 그대로 보여준다. 관찰문 입력·외부 GPT 프롬프트 구조는 **2026-08-13에 걷어냈다** |
| 상담 도우미 | ✅ 테마 12종 UGC(도움됨 1인 1회·신고·숨김) · 성향 가이드 · **스토킹처벌법 주의** |
| 자료실 | ✅ **구획 폐지 — 폴더 한 줄기**. 아카이브 2폴더 · 말씀광장 · 천지일보. **자료 목록은 게시판 표**(번호·추천·제목·작성일시·작성자, 최신·인기·추천·조회 정렬 — 강의 도우미와 공용). 파일 원본은 R2 대기 |
| 셸 (개편분) | ✅ **사이드바 접기(68px 레일 · 호버 펼침 · 핀 고정, `zion_ark_nav_pinned`)** · **로고 → 메인** · **검색 갈래 다중 선택 필터**(체크박스 드롭다운) |
| 검색 전반 | ✅ **띄어쓰기 무시 매칭** — 「천국비밀」로 찾아도 원문 「천국 비밀」이 걸린다 (`lib/text-match.ts`). 어록 뽑기는 **전부 보기**(종전 20건 컷 해제) |
| 공지 · 어록 | ✅ 독립 대주제 |
| ~~사명자 심방 도우미~~ | ❌ **2026-08-13 리드 지시로 없앰** (`/care` → 메인 리다이렉트). 번아웃 척도 원본을 못 받아 자가진단이 계속 비어 있었다 |
| ~~관찰 필요~~ | ❌ **2026-08-13 리드 지시로 없앰** (`/signals` → 수강생 현황). 출결 신호 계산은 등급 판정에 계속 쓰인다 |
| **사이트 이용 현황 (`/usage`)** | ✅ **2026-08-15 신설 · 지파 신학부장 이상 전용**. 오늘 들어온 사람 · 최근 7/14/30일 이용자 · 날짜별 막대 · **지파별·역할별 이용자**. ⚠️ 집계만 보이고 누가 들어왔는지는 안 나온다(불변식 2) |
| 12지파 선교센터 | ✅ **지파 신학부장 이상 전용** (2026-08-14 FB-10 — 메뉴 숨김 + 라우트 가드, 역할 목록은 `permissions.ts` `MISSION_CENTER_VIEW_ROLES`). 위치 표기 **'구' 단위까지** — 도로명·층·길찾기 링크를 걷어냈고 핀도 구 단위로 찍는다(같은 구는 옆으로 벌림). 금색 핀 · 확대하면 기수 · 눌러서 **진도**. 대전(맛디아) 2곳 |
| 마이페이지 | ✅ **개인 주간 스케줄러**(담당 수강생 생일 자동) · 즐겨찾기 · 열람 기록 · **화면 밝기** · **개인 메모장** — 2026-08-21에 저장 단추 방식이 됐고 메모가 **쌓인다**(「메모 기록 보기」 팝업에서 고치고 지운다) |
| 셸 | ✅ 네이비 테마 · **다크 모드**(헤더 단추 · 기기 설정 따르기) · **화면 전환 효과** · **NEW 뱃지** · **오늘의 어록** · **배경 음악**(S-POP 6곡) · 맨 위로 |
| 검색(Ask AI) | ✅ **어절 단위 · 조사/질문어 처리 · 점수순 · 띄어쓰기 무시 · 갈래 다중 선택 필터**. 용어집까지 훑는다. AI 응답은 백엔드 대기 |

## 다음에 할 수 있는 것

0. **다음 세션에서 이어갈 것**:
   - 리드가 새 화면(출석 상세 진도별·요일 구간 편집·드래그)을 눈으로 보고 확인
   - 6개월차가 **몇 주차인지** 확정 (지금은 사용자가 직접 주차를 적는다 — 27주쯤으로 예시)
   - 요일 구간을 **기수별로** 두는 지금 구조로 충분한지 (분반마다 다를 여지가 있는지)
   - **비밀글에 지파 관리자가 답글을 달 수 있어야 하는지** — 2026-08-15에 열람만 넓혔다.
     답글은 종전대로 총회 신학부장만이다(`canReplyBoard`). 자기 지파 글을 보면서 답은 못 다는
     상태라, 답글도 넓힐지 리드가 정하면 그 함수 한 곳만 고친다
   - ~~일요일 해석~~ → **2026-08-15에 확정됐다** (아래 「수업 요일」 항목)
0-1. **피드백 대장 2026-08-14 회차 — 전 세션 반영 완료** (원문:
   `시온 아크 프로젝트/시온아크-2026-08-14-피드백대장-작업지시문.md`).
   리드가 Q-01(회차↔진도: 목업 신설) · Q-02(교안: 추천안 2단 체계) · Q-03(교분기:
   표준+지파 보충 2계층)을 확정해 13건 전부 끝났다. BUG-01·02는 파트 B 머지(af2863d)가
   이미 고쳐 재현 불가 확인만 했다. 회차↔진도 매핑은 `src/content/curriculum-mock.ts`
   (교체 경계 — 시범 값), 개인정보 검토 보고서는 `docs/PRIVACY-REVIEW-2026-08-14.md`.
   ⚠️ 남은 백엔드 몫: 비밀글·유효조회·지파 보충본 스코프의 **서버 판정**(클라이언트
   거름은 전부 편의일 뿐이다) · 감사 로그 열람 화면 · 법무 자문 발송(보고서 §6)
1. ⛔ **강의 도우미 4항목의 나머지 세 칸 — 원문이 필요하다** (2026-08-18 착수분의 남은 몫).
   첫 칸 「시기 따른 관리 방향」은 넣었다(`Lessons.tsx`의 `LevelGuidance` — 초·중·고 정의와
   「인교섬 미션」 정의를 원문 그대로 나란히 놓는다. 인교섬은 계정이 없으므로 **강사·전도사가
   무엇을 부탁할지 보는** 화면이다). 남은 셋은 **원문을 못 받았다**:
   - 진도에 따른 질문 · 무신앙 예상 질문 · 신앙인 예상 질문
   - **지어내지 않았다** — 교리 콘텐츠 창작이 되고(불변식 5), 빈 칸만 세우면 껍데기가 된다
   - ⚠️ 항목 이름 자체가 2차 기록(`HISTORY`)에서 온 것이다. **지시문 §2-2 원문이 이 저장소에
     없다** — 1단계 착수지시문 v2에는 §2-2가 없다. 원문을 받을 때 **항목 이름도 함께 확인**한다
2. **보강 자료 7개 폴더 채우기** — 원문을 받으면 바로 넣는다 (자리는 `/makeup`에 있다)
3. **녹취 정리에 실제 받아쓰기를 붙일지 정하기** — 지금은 밖에서 받아쓴 글을 넣는 구조다.
   사이트가 직접 받아쓰려면 **백엔드에서 Whisper API를 부르는 수밖에 없고 1시간당 수백 원**이
   든다. 리드가 비용을 승인하면 그때 붙인다 (`/digest`의 입력 자리는 그대로 쓸 수 있다)
4. ~~분반·보강도 제 카테고리 안에서~~ → **2026-08-18에 넣었다** (`/makeup`).
   `TeachingLibrary`와 같은 짜임이고 몸통은 `FolderLibrary` 한 벌을 그대로 쓴다.
   ⚠️ 자료실에서도 계속 보인다 — 자료실이 전체 보기를 겸하기 때문이다(강의 도우미와 같은 취급)
5. **상담 도우미에 소주제 미루기를 켤 수 있게 됐다** — 종전에는 소주제 안의 등록 모달이
   `contain: paint`에 갇혀 못 켰는데, 2026-08-13에 모달을 `Portal`로 빼서 그 제약이 풀렸다.
   `Counseling.tsx`의 `<Accordion>`에 `deferOffscreen`을 주고 **모달이 화면 가운데 뜨는지**
   확인하면 된다 (본문 18,000px짜리 화면이다)
6. **팀원 검수 응답 받기** — 이슈 #2(파트 B)는 넷 다 반영해 올렸다. **#1·#3·#4·#5는
   아직 응답이 없다**(파트 A·C·D·E). #3은 세팅 완료만 올라와 있다

## 사람이 주지 않으면 못 하는 것

- **원본**: 중등 교안 · **신천지 절기 날짜**(달력 자리는 `content/scj-seasons.ts`에 비어 있다) ·
  성향 상담 내부 원문 ·
  강별 PPT/영상 실제 주소 · **아카이브 파일(실상 뮤지컬 영상 · 계시록 삽화 —
  내려받기 링크가 붙어야 열린다)** ·
  **나머지 11개 지파 선교센터 주소** (대전 두 곳은 2026-08-11에 **맛디아 지파**로 확정)
- **주소**: 상담 GPT(커스텀 GPT) 링크 — 상담 도우미 ⑫ · 성향 분석 · 녹취 정리에서 대기
- **수치**: 기수요약 퍼널 지표(신카수 31명 등)와 사명자 현황 이름은 **시범 값**이다 —
  실제 값을 받으면 `cohort-mock.ts`의 `COHORT_FUNNEL`·`COHORT_STAFF`만 갈면 된다
- **정책**: 마이페이지 열람 기록 보관 기간(권장 90일) · 상담법 신고 처리 기준 ·
  수강생 화면 열람 기록을 켤지 ·
  ⚠️ **외부 AI 동의를 어떻게 받을지** — 수강 신청서에 넣을지 기수 단위로 받을지가 미정이다.
  코드는 `ConsentState`(`granted`/`denied`/`unknown`) 자리를 만들어 뒀고 **`unknown`이면
  보내지 않는다.** 실제 동의 데이터가 붙기 전까지 외부 AI로는 아무것도 나가지 않는다
- **확인**: `OPEN_QUESTIONS.md` §C의 C-1(신앙전환↔영적전환) · C-2(주전도사 역할 코드) ·
  C-3(인도자·섬김이 구분) · C-5(회 구분 도입) · C-7(마팔 연동) · §E(텔레그램 발송)

⚠️ **용어 정의는 전부 받았다** (2026-08-08). 원문은 `src/content/glossary.ts`,
연결 관계는 `docs/decisions/GLOSSARY.md`. 정의가 왔어도 **코드 값으로 굳히지 않는다**.

## 지금 구조에서 알아야 할 것

- **수업 요일은 기수 도중에 바뀐다** (2026-08-14 리드 지시 — 종전 「8개월·월화목 고정」을
  이 항목이 대체한다). 개강~6개월차 월·화·목, 6~8개월차는 **일·수·목**이다
  (2026-08-15 리드 확정: **월요일 수업이 일요일로, 화요일 수업이 수요일로 옮겨진다**).
  그래서 「N주차부터 이 요일」 **구간 목록**(`ClassWeekdayPeriod[]`)으로 두고
  **전체 현황 › 기수 일정**에서 사용자가 고친다(담당 기수의 강사·전도사).
  두 조합은 **프리셋 단추**(「월·화·목」·「일·수·목」)로 한 번에 넣는다
  - 요일을 읽는 곳은 전부 `weekdaysOfWeek(주차, 구간)`을 거친다 — `CLASS_WEEKDAYS`
    상수를 직접 보지 않는다(그건 기본값일 뿐이다). 출결 격자 칸·회차 번호·총 수업 횟수가
    전부 따라 움직인다
  - ⚠️ **주차 번호·주차 라벨(그 주 목요일 기준)은 안 바뀐다.** 세 요일 조합에 목요일이
    다 들어 있어 이 전제가 살아 있다 — 저장된 주차 기록이 끊어지지 않는다
  - ⚠️ **일요일은 그 주의 첫날(월−1)이다** (2026-08-15 확정 — 종전 「그 주의 끝(월+6)」을
    이 규칙이 대체한다). 월요일 수업이 **하루 앞당겨져** 일요일이 된 것이므로 일·수·목
    차례가 된다. `offsetInWeek`과 `weekNoOf` **두 곳이 같은 규칙**을 쓴다 — 어긋나면
    구간 첫 주의 일요일 수업이 앞 주차로 밀려 회차에서 통째로 빠진다.
    Node 검산으로 확인한 것: 날짜를 훑어 세는 길과 주차별로 더하는 길이 **둘 다 105회**,
    주차 라벨은 종전과 동일(13주=5월 4주 · 18주=7월 1주), 27주차 첫 수업 8/30(일)
  - 회차 번호는 **앞 주차들의 실제 수업 수를 더해** 매긴다 — 주당 회차가 구간마다
    달라질 수 있어 `주차×3`으로 계산하면 어긋난다
- **건의·의견 게시판 비밀글은 셋이 본다** (2026-08-15 리드 확정 — 종전 「총회 신학부장만」을
  넓혔다): **작성자 본인 · 총회 관리자(`headquarters_admin`·`content_admin`) · 작성자와
  같은 지파의 `tribe_admin`**. 판정은 `permissions.ts`의 `canReadSecretPost` 한 곳이다.
  - 지파 대조 기준으로 `BoardPost.createdByTribe`를 **새로 남긴다**(전방 추가 — 옵션).
    ⚠️ **값이 없는 옛 글은 지파 관리자에게 안 보인다** — 비밀글이라 모호하면 닫는 쪽이다
  - ⚠️ **답글은 종전대로 총회 신학부장만이다**(`canReplyBoard`) — 열람만 넓혔다
  - ⚠️ **서버가 같은 판정을 다시 해야 한다.** 시범 로그인은 인증이 아니라 세션의 지파를
    브라우저에서 고칠 수 있다 — 지파 대조를 클라이언트만 믿으면 남의 지파 비밀글이 열린다
- **「지금 우리 기수는?」 탭이 생겼다** (2026-08-15 리드 지시 · `pages/CohortNow.tsx`).
  **단계 기준표를 기수 단위로 뒤집어 본다** — 수강생 상세의 단계 항목이 「한 사람」을 세로로
  본다면 여기는 「우리 기수가 어느 항목에 강하고 어느 항목이 약한가」를 항목 가로로 본다.
  - 항목을 누르면 **잘 되는 분 / 처지는 분** 명단이 펼쳐지고, **이름을 누르면 상세 팝업**이 뜬다
  - 계산은 `lib/cohort-strength.ts` 한 곳 — **새 판정을 만들지 않는다.** 담당자가 매긴 단계 항목
    점수(0~5)의 평균과 정렬뿐이다(불변식 4·5)
  - ⚠️ **미채점은 「아직 안 봄」**이고 약점에 넣지 않는다 — 모르는 것을 약점으로 세면 사실이 아니다
  - ⚠️ 강점 70%↑ · 약점 40%↓는 **화면 표시용 경계**이지 기준표의 A/B/C 등급이 아니다
    (기준표 등급은 섹션 합계로 매긴다). 바꾸려면 `STRONG_MIN`·`WEAK_MAX` 두 상수만 고친다
  - ⚠️ 점수는 지금 **시범 값**이다(`demoChecklistProgress` — 교체 경계). 담당자가 실제로 매긴
    점수가 있으면 **그쪽이 이긴다.** 항목마다 치우침을 준 이유가 있다 — 사람 편차만 쓰면
    항목 평균이 한 구간(54~65%)에 몰려 **강점도 약점도 하나도 안 나온다**(처음에 그랬다)
- **기수 현황은 가로 탭이고, 그 다섯 항목이 사이드바에서도 펼쳐진다** (2026-08-15 리드 수정 지시).
  잠깐 아코디언으로 바꿨다가 **되돌렸다** — 리드 지시는 「왼쪽 대카테고리에서 펼쳐지게,
  우측 큰 창에서는 지난번처럼 가로 열로」다.
  - ⚠️ **이름·순서 정본은 `CohortStatus`의 `COHORT_TABS`**다. `nav.ts`가 거기서 읽어
    사이드바 항목을 만든다 — 두 곳에 적으면 어긋난다
  - ⚠️ 탭 상태는 **주소(`?tab=`)가 정본**이다. `useState` 초기값으로 두면 사이드바에서 다른
    항목을 눌러도 화면이 안 바뀐다(컴포넌트가 살아 있어 초기값이 다시 안 읽힌다)
- **이름이 바뀐 것들** (2026-08-15 리드 지시 — 경로·저장 구조는 그대로다):
  - 「교분기」 → **「흐름교육」**. ⚠️ **폴더 저장값은 `교분기 초등`… 그대로**이고 **표기만**
    바뀌었다(`folderLabel`) — 값을 바꾸면 이미 저장된 자료가 폴더에서 빠진다(불변식 10)
  - 「건의 · 의견」 → **「게시판」** · 「수강생 현황」 → **「AI 성장 추천」**
  - 기수 비교의 「같은 회차에서 견주기」 → **「비교」**
- **없앤 화면 하나 더**: 「강의 녹취 정리」(`/digest` → 강의 도우미로 리다이렉트).
  ⚠️ 화면과 함께 `lib/transcript-digest.ts` · `transcript-parse.ts` · `bible-refs.ts`도 지웠다 —
  그 화면만 쓰던 것들이다. 되살릴 때는 git 이력에서 넷을 함께 꺼낸다
- **초·중·고에 「우수 판서」가 생겼다** (`BOARD_WRITING_FOLDERS`) — 잘 쓴 판서를 모으는 폴더다.
  자료 몸통은 종전대로 `FolderLibrary` 한 벌을 쓴다
- **수강생 관리 개편** (2026-08-15):
  - 「AI 성장 추천」(`/students-dashboard`) — 줄을 누르면 상세의 추천 액션이 **교안·보강 자료로
    바로 이어진다**(FB-08의 `suggestion-links` 그대로)
  - **분반 띠**가 생겼다 — 「전체 분반」에서도 분반별로 나뉘어 보이되 전체가 다 나온다
  - **인교섬 연락처**가 이름 아래 뜬다. ⚠️ **수강생 전화와 같은 마스킹**이고, 해제는 감사 로그에
    남는다. 값이 없으면 이름에서 만든 **시범 번호**가 자리를 채운다(가짜 번호 — 불변식 6)
  - ⚠️ **「성장 추세」 꺾은선은 뺐다** — 과거 점수 이력이 없어 지금 점수로 지어낸 추정선이었다
- **마이페이지에 「팔로우한 강사」**가 생겼다 — 이름을 누르면 `/library?q=이름`으로 가
  그 사람의 자료만 걸린다(검색이 작성자도 찾는다)
- **사이트 이용 현황(`/usage`)이 생겼다** (2026-08-15 리드 지시 — 「아크 사이트를 많이 쓰는지 확인」).
  **지파 신학부장 이상만** 본다(`canViewSiteUsage` · 메뉴 숨김 + 라우트 가드).
  방문은 `Layout`이 **계정·날짜 하나당 한 줄** 남긴다(`zion_ark_site_visits`).
  - ⚠️ **집계만 화면에 낸다**(불변식 2) — 저장된 이름은 재방문을 거르는 데만 쓴다
  - ⚠️ **지금 값은 그 브라우저에 쌓인 것뿐이다** — 실연동 시 서버가 세션에서 기록해야
    전국 집계가 된다. 화면에도 이 한계를 적어 뒀다
- **출석 현황(기수현황 › 출석 상세)은 2026-08-15에 배치를 갈아엎었다** (리드 피드백 9건):
  - **상태·보강포함·대면만이 이름 바로 옆**이다 — 종전에는 69칸 건너 맨 오른쪽이었다
  - **최근 회차가 왼쪽**이고 **맨 오른쪽이 1주차**다. 열자마자 최근 출결이 이름 옆에 보인다.
    **주 안의 요일도 함께 뒤집힌다**(목·화·월) — 연속 결석이 한 덩어리로 보이게 한 것이다
    (2026-08-15 추가 지시). 「최신순 / 오래된순」 단추로 통째로 뒤집는다
  - 회차 칸에 **날짜**(`8/3`)와 **강**(`중46강`)을 적고, **요일을 골라** 볼 수 있다
  - 칸을 넓혀 **한 화면에 4주** (종전 8주). ⚠️ 폭 상수 `colW`는 1280px에서 4주가 딱 들어가게
    잡은 값이다 — 더 넓히면 4주째가 잘린다
  - **출석한 사람이 위, 결석이 아래**이고 **분반별 가로 띠**로 묶는다.
    ⚠️ 2026-08-14의 「출석률 낮은 사람이 위」를 **뒤집은 것**이다
  - **결석(X)·보강 예정(▽) 칸을 누르면 보강 기록**을 남긴다 — 보강일 · 계획/완료 · 메모.
    ⚠️ **출결 원본은 그대로다**(불변식 3) — 원본 mark 옆에 붙는 별도 기록이고,
    `studentFeedback`(kind `makeup` + `sessionNo`·`makeupState`) **한 곳**에 저장돼
    수강생 상세의 「보강 · 상담 메모」가 같은 것을 읽는다. 두 벌로 나눠 저장하지 않는다
- **회차 하나가 강 하나다** (2026-08-15 리드 지시 — 종전에는 초등 23강을 105회차에 고르게 펴서
  한 강이 4~5회차에 걸쳐 있었다). `curriculum-mock.ts`의 `CURRICULUM`이 105회를
  **초등 23강 → 중등 60강 → 고등 22강** 차례로 채운다.
  ⚠️ **중등은 원문이 없어 제목이 빈 칸**이다 — 번호만 매겨 뒀고, 원문이 오면 그 한 곳만 채운다
- **과수 제목 정본은 `src/content/curriculum-titles.ts` 하나다** (2026-08-15 리드 전달 —
  초등 25 · 중등 25 · 고등 23(계 1~22장)). 진도가 보이는 자리는 전부 여기서 읽는다 —
  교안 원문 파일명·강 제목에서 다시 뽑지 않는다(그렇게 하다 화면마다 표기가 갈렸다).
  - ⚠️ **문구를 다듬지 않는다**(불변식 5). 「세가지 이스라엘」 띄어쓰기도 원문이다.
    리드가 오기라고 확인해 준 「중등18」 하나만 뺐다
  - ⚠️ **회차 수 ≠ 강 수.** 초등은 한 과수를 나눠 진행하고(기수 재량 · 「산(1)」「산(2)」),
    중등은 목록 중 몇 개만 뽑아 하고, 특강이 끼어든다. `CURRICULUM`(회차→강)은 **시범 값**이고
    남는 회차는 `undecided`(화면에 **「미정」**)다 — 없는 진도를 지어내지 않는다
- ⛔ **연속 번호 표기 금지 — 학원법 때문이다** (2026-08-15 리드 지시).
  「초등1·2·3」·「초1강」·「중12강」처럼 단계에 연속 번호를 붙이면 학년·학기 편성처럼 읽힌다.
  화면에는 **단계(초·중·고) + 과수 제목**만 낸다.
  - 번호(`lessonNo`)는 **데이터로 남긴다** — 차례·회차 짝짓기에 쓰고 화면에만 안 낸다
  - **회차(`N회차`)는 쓴다.** ⚠️ 다만 **회차를 「N강」으로 적지 않는다** — 주간계획이 입력은
    「회차」로 받고 표기는 「N강」으로 내던 것을 고쳤다
  - 검색·자료 모으기의 출처 표기(「초등 교안 3강」)도 과수 제목으로 바꿨다
  - 강의 도우미의 과정 탭에서 **개수도 뺐다** — 정본 25과수와 탑재된 교안 23강이 달라
    숫자를 적으면 어긋난 값이 보인다
- **진도 요약 표기가 생겼다** (2026-08-18 리드 전달). 좁은 칸(출석 격자·달력·표)에서
  「빛 · 등대와 소경 · 귀머거리 · 예복」이 **「빛 ~ 예복」**으로 나온다.
  정본은 `curriculum-titles.ts`의 `ELEMENTARY_SHORT_TITLES`(25)·`MIDDLE_SHORT_TITLES`(25),
  고등은 `chapter`(「계 2장」 — 이번에 띄어쓰기를 통일했다)다.
  - 종전에는 사이트가 **여덟 글자에서 기계적으로 잘라** 「빛 · 등대와 …」로 뜻이 끊겼다.
    이제 **사이트가 줄이지 않는다** — 목록에 없으면 원 제목 그대로다
  - ⚠️ **차례가 밀리면 남의 줄임말이 붙는데 화면은 멀쩡히 그려진다.**
    `curriculum-titles.test.ts`가 개수·빈칸·표본 몇 개를 지킨다(9건)
  - ⚠️ 「열쇠」만 **일부러 길어졌다**(→ 「천국과 열쇠」). 두 글자로는 무슨 열쇠인지 알 수 없어서다.
    테스트에 예외로 못 박아 뒀다 — 또 길어진 항목이 생기면 그 자리에서 한 번 묻게 된다
- **초·중·고를 색으로 가른다** (2026-08-15 리드 지시 · **초등은 2026-08-18에 하늘색 → 초록**).
  색값은 `index.css`의 `@theme`에 토큰(`--color-level-*`)으로 있고, 화면은
  `LEVEL_TONE`(뱃지 한 벌: 옅은 바탕 + 진한 글자)만 가져다 쓴다.
  - 실측 대비: 밝은 화면 5.67~8.89 · 어두운 화면 7.77~8.59 (전부 AA 통과 — 2026-08-18 초록 반영본)
  - ⚠️ 초록 hue를 **145**로 잡은 것은 상태색 `emerald`(165)와 벌리기 위해서다 —
    「강점」·「상담」 표시와 단계 뱃지가 같은 화면에 있어 너무 가까우면 섞여 보인다
  - ⚠️ 고등(남색)은 주색(`zion`)과 비슷해 **일부러 보라 쪽으로 기울였다** — 안 그러면 버튼·머리글과 섞인다
- **일정 묶음 (2026-08-18 리드 지시·승인 아이디어 ①~④ 전부 반영)**:
  - **① 기수 일정 겹쳐 보기** — 내 일정(주간·월간)에 기수 달력 항목이 **읽기 전용 배경**으로
    깔린다(옅은 남색 줄 · 「기수 일정 겹침/숨김」 토글). 고치는 것은 기수 달력에서만
  - **② 메인에 「오늘」 카드** — 기수 일정·내 일정(반복 포함)·담당 수강생 생일을 한 줄로.
    없으면 「적힌 일정이 없습니다」라고 말한다(빈 자리를 감추지 않는다)
  - **③ 내 일정에 담기** — 기수 달력 팝오버의 항목마다 달력+ 아이콘. 「[기수] 상담 — …」으로
    복사되고 같은 날 같은 제목은 두 번 안 담긴다. 캘린더 내보내기(하루 전 알림)로 이어진다
  - **④ 매주 반복** — 일정 추가에 「매주 반복」 체크. 표시는 ↻, 지우면 반복 전체가 지워진다.
    캘린더 내보내기는 RRULE(FREQ=WEEKLY)로 나간다 — 회차를 일일이 내보내지 않는다
  - **마이페이지 상단 「개인 메모장」** — 계정당 한 장, 본인만 보고 **적는 대로 자동 저장**.
    내보내기가 없어 반출 경로도 없다
  - **기수 달력에 「기수 회의록」** — 주간계획과 같은 권한(해당 기수 강사·전도사만 쓴다).
    ⚠️ 기수 공유 기록이라 `scanPII`가 걸리면 **저장을 막는다**(안내만으로는 긴 글에서 놓친다)
  - 「이름은 적지 않습니다」 안내 문구는 리드 지시로 화면에서 뺐다 — **원칙은 그대로**이고
    (개인 일정 반출·기수 공유 기록), scanPII 경고가 실수를 잡는다
- **기수 달력에 상담·심방 종류가 생겼다** (2026-08-17 리드 지시). `PlanEntryKind`에
  `counsel`·`visit` **전방 추가** — 저장된 항목은 그대로 유효하다. 업로드 양식·매핑에도 넣었다.
  - ⚠️ **상담·심방 계획에 수강생 이름을 적지 않는다** — 달력은 기수 공유 화면이다.
    입력 폼에 `scanPII` 경고를 붙였고(게시판과 같은 방식 — 정규식은 그물이고 안내가 먼저다),
    누구를 만났는지는 수강생 상세의 「보강 · 상담 메모」에 남긴다
- **마이페이지 「내 일정」에 월간 보기가 생겼다** (2026-08-17 리드 지시 — 종전 「내 주간 일정」).
  주간 ↔ 월간 토글이고 **같은 저장소를 두 눈금으로 보는 것뿐**이다 — 월간에서 넣은 일정이
  주간에도 그대로 있다. 월간 칸을 누르면 같은 팝오버로 추가·삭제하고, 생일·캘린더 내보내기도
  보이는 범위(주/월) 그대로 동작한다. 좁은 화면에서는 칸 제목 대신 「N건」으로 줄인다
- **마이페이지 「최근 본 것」이 경로 대신 화면 이름을 보인다** (2026-08-17 리드 지시 —
  `/plan`이 아니라 「기수 현황 · 월간·주간 계획」). 이름은 `nav.ts`의 `pageLabelOf`가
  **메뉴 정의(NAV_GROUPS)에서 파생**한다 — 화면 이름을 딴 곳에 다시 적으면 메뉴와 어긋난다.
  - ⚠️ **저장 구조는 그대로다** — 기록에는 여전히 경로(식별자)만 남고, 이름은 그릴 때 찾는다.
    메뉴 이름이 바뀌면 지난 기록도 새 이름으로 보인다
  - 메뉴에 없는 딥링크(수강생 상세·자료 검색 등)는 `PAGE_LABEL_FALLBACKS`가 받고,
    그래도 못 찾으면 「(지금은 없는 화면)」이다 — 코드 경로를 그대로 내보이지 않는다
  - 같은 함수의 자료 링크가 폐지된 구획(`?section=`)으로 가던 것도 `?open=<id>` 딥링크로 고쳤다
- **화면 문구를 전면 점검했다** (2026-08-16 — 리드 지시 「AI 같은 말투 수정」). 규칙은
  `CLAUDE.md` 「화면 문구 규칙」이 정본: 본문 이모지 금지 · 챗봇식 권유 금지 ·
  외래어 명명 회피(액션→활동) · 긴 안내문은 문단 끊기 · 확인 버튼은 「저장」.
  고친 곳: 게시판 빈 화면, 검색창·어록 안내 문구, 상담 도우미 유의 표시(⚠️→「유의」),
  출석 격자 부제·하단 안내(3문단), 요일 편집기(고침→저장), 사이트 이용 현황,
  주간 스케줄러, 별점 배너, AI 성장 추천(추천 액션→추천 활동)
- ⚠️ **가로 스크롤 자리는 차례가 바뀌면 되돌린다** (2026-08-15 — 리드가 「최신순·오래된순
  누를 때 칸이 움직인다」로 짚었다). `useDragScroll(resetKey)`에 **차례·축·요일 필터**를 이어
  붙여 넘긴다 — 안 되돌리면 옛 스크롤 자리에 새 칸이 걸려 붙박이 칸 아래로 잘려 보인다
- **출석 현황은 축이 셋이다** (2026-08-15 리드 지시로 「요일별」을 더했다):
  **진도별**(주차로 묶음 · 기본) · **요일별**(같은 요일끼리 묶음 — 「목요일마다 빠진다」가 한 줄로
  보인다) · **주차별**(한 주 한 칸 요약).
  ⚠️ 요일별에서는 **특강을 맨 뒤 한 묶음**으로 모은다 — 특강은 요일이 제각각이라 요일 묶음에
  넣으면 흐름이 끊긴다
- **특강을 주차마다 더한다** (2026-08-15 리드 지시). 아무 요일에나 만들 수 있고, 그 칸의
  출결은 **담당 강사·전도사가 눌러서** 기록한다(미입력→출석→결석 순환).
  - ⚠️ **출결 원본 읽기 전용(불변식 3)에 걸리지 않는다** — 특강은 원본 시트에 없는 것이라
    사이트가 기록한다. **정규 회차 출결은 여전히 못 고친다**
  - ⚠️ **출석률에는 기본으로 안 들어간다** (「메인강의만」). 「특강 포함해서 셈」 단추를 켜면
    개인 비율과 평균에 함께 든다 — 정규 비율과 **같은 창(최근 8주)**을 쓴다
  - ⚠️ **수업날에 특강식으로 한 수업은 특강으로 만들지 않는다** — 정규 회차 칸에 그대로 체크한다
    (등록 창에도 이 문구가 있다)
- **기수 비교(이름은 「비교」)는 같은 회차에서 견준다** (2026-08-15 리드 지시). 달력·동월 비교를
  버렸다 — 월 초 개강 반과 월 말 개강 반은 진도가 달라 견줄 수 없기 때문이다. 슬라이더로 회차를
  고르면 **그 회차에서 각 기수가 어땠는지**가 나온다(이미 종강한 앞선 기수도 그 회차 값이 보인다).
  - **기수 이름 옆에 과정 개월수**가 붙는다 — 지파마다 6~8개월로 달라 속도를 함께 봐야 한다.
    ⚠️ **우리 기수의 개월수는 전체 현황의 일정에서 온다**(`effectiveSchedule`) — 화면에서
    개강·종강을 고치면 따라 움직인다. 목업의 `months`를 쓰면 고친 일정과 어긋난다
  - **12지파 전체 비교는 이제 누구나 본다** (종전 총회 범위 계정만) — 내보내는 것이
    기수명과 출석률 집계뿐이라 불변식 2에 걸리지 않는다
  ⚠️ 회차별 출석률은 `cohort-mock`의 `sessionRateOf`가 만드는 **시범 값**이다 — 실연동 시
  이 함수만 갈아 끼운다(교체 경계)
- **출결을 보는 표·막대는 붙잡고 끌어서 넘긴다** (`src/lib/drag-scroll.ts`, 2026-08-14).
  출결 격자·주간 흐름 막대·등급×분반 교차표·수강생 현황 표에 붙어 있다.
  ⚠️ **3px 문턱**이 있어야 표 안 버튼을 누를 수 있고, 끈 뒤에는 뒤따르는 click을 한 번
  **삼켜야** 손 뗀 자리의 버튼이 눌리지 않는다. 스크롤이 필요 없는 폭에서는 아예 안 켜진다
  (그래서 넓은 화면의 수강생 현황 표에서는 드래그가 안 걸리는 것이 정상이다)
- 기수 일정 산수는 전부 `src/lib/cohort-calendar.ts` — 수업일 세기·주차·진행률·`newcomerEndOf`(종강+2주).
  일정을 화면에서 고치면 `zion_ark_schedule_overrides`에 얹히고 **읽는 쪽은
  `effectiveSchedule()`을 거친다** (홈 일정 카드·진행률·달력 마커가 전부 따라온다).
  - ⚠️ **주차 라벨(「6월 2주」)은 그 주 목요일 기준이다.** 저장된 주차 사유
    (`zion_ark_week_notes`)가 이 라벨로 조인된다 — 규칙을 바꾸면 기록이 통째로 끊어진다.
    Node 검산 15건 + 실제 조인 생존 확인까지 했다
  - ⚠️ 수업 회차(105회)에 **공휴일을 빼지 않는다** — 목업이 3/2(대체공휴일)를 개강일로 쓰고
    있고 실연동 정본은 출결 시트다. 사유는 `scheduleSummary` 주석
  - 목업 실적은 **23주차(8월 1주)에서 멈춰 있다**(`DONE_WEEKS`) — 시연 한계
- **새 localStorage 키 3개** (2026-08-13): `zion_ark_nav_pinned`(사이드바 고정) ·
  `zion_ark_schedule_overrides`(일정 수정) · `zion_ark_material_views`(자료 조회수)
- **메인(`/`)과 전체 현황(`/overview`)은 다른 화면이다** (2026-08-13 리드 지시로 분리).
  메인은 **카테고리를 고르는 자리**, 전체 현황은 **점검자용 수치 요약**이다.
  ⚠️ 카테고리 타일은 `visibleNavGroups`에서 **파생**한다 — 메인에 목록을 다시 적으면
  사이드바와 어긋난다(자료실 폴더가 두 군데 뜨던 2026-08-09 사고와 같은 자리)
- **없앤 화면 둘** (2026-08-13 리드 지시): 「관찰 필요」(`/signals` → 수강생 현황으로 리다이렉트) ·
  「사명자 심방 도우미」(`/care` → 메인으로). ⚠️ **`lib/attendance-signals.ts`는 남는다** —
  등급 판정과 수강생 상세의 「주의 포인트」가 그 값을 쓴다. 화면만 걷어낸 것이다.
  되살릴 때는 git 이력에서 `pages/Signals.tsx`·`pages/Care.tsx`를 꺼낸다
- **모든 검색은 띄어쓰기를 무시한다** (`src/lib/text-match.ts`, 2026-08-13 리드 지시).
  `looseIncludes`·`looseCount`·`looseIndexOf` 셋을 상단 검색·자료실·교안·시리즈·상담 사례·
  어록이 함께 쓴다.
  ⚠️ **자리(index)는 `looseIndexOf`로 원문에서 다시 찾는다** — 공백을 지운 문자열의 위치는
  원문과 어긋나 스니펫이 엉뚱한 데서 잘린다
  - 붙여 쓴 한 낱말(「천국비밀」)은 **그 연속이 원문에 있어야** 걸린다. 어록에는 그 표현이
    없어서 0건이 나오는데, 화면이 「띄어서 찾아 보라」고 안내한다
- **인기 교안은 「조회 대비 좋아요 비율」이다** (2026-08-15 리드 확정 — 종전 30일 종합 점수를
  대체한다). 조회수는 **계정당 1회**만 오르고(`viewedBy`), 본 사람 중 좋아요를 누른 비율로
  줄 세운다. 조회 5건 이상 · 비율 50% 이상이면 목록·상세에 **「인기」 마크**가 붙는다.
  - **좋아요가 세 갈래**다 — 무신앙·무신론자에게 / 왜곡 씻기에 / 신앙 성장에
    (`MATERIAL_LIKE_KINDS`). 갈래마다 1인 1표, 여럿 고를 수 있다.
    ⚠️ 게시판 표의 「추천」 수(`helpfulBy`)는 **세 갈래의 합집합**으로 함께 갱신된다 —
    한쪽만 고치면 표와 정렬이 어긋난다
  - **작성자로도 검색**되고, **작성자 팔로우**와 「팔로우한 작성자만」 필터가 있다
  - ⚠️ 유효 조회(30초 체류, FB-04)는 **기록만 남고 산정에서 빠졌다** — 조회수가 계정당 1회가
    되면서 조작 방지 몫을 그쪽이 맡았다. 저장값은 지우지 않았다(불변식 10)
- **우수 교안 지정은 지파 신학부장이 한다** (2026-08-15 리드 지시 — 2026-08-05 확정값
  「총회 신학부장만」을 대체한다. `canToggleFeatured`).
  ⚠️ **「지파 공유 승격」과 주체가 겹친다** — 둘 다 지파 신학부장이 누른다. 합칠지는 리드 확인 대기다
- **자료 목록은 `FolderLibrary` 한 곳이 게시판 표를 그린다** — 자료실·강의 도우미가 같이
  쓴다. 추천은 `LibraryMaterial.helpfulBy` **1인 1표 토글**(상담 사례와 같은 계약),
  조회수는 상세를 **여는 클릭에서만** +1(StrictMode 이중 실행 함정 회피).
  ⚠️ **행(tr)을 버튼으로 만들지 않는다** — 행 안에 즐겨찾기 버튼이 있어 중첩이 된다
- **로고는 리드가 준 그림 파일이다** (2026-08-13). `src/assets/logo-ark.png` — 원본 JPG의
  **흰 배경을 투명으로 빼고 여백을 잘라** 384×228로 만든 것이다(원본은 git 제외인
  `Design/logo-ark-source.jpg`). 다시 만들 일이 생기면 요령은 셋이다:
  - 흰 바탕 선화는 **`alpha = 255 - min(R,G,B)` + 흰색 역합성**으로 빼야 가장자리가 안 계단진다.
    임계값으로 자르면 지저분해진다
  - ⚠️ **`?inline`으로 base64를 번들에 박는다.** `public/`에 두면 **단일 HTML 프리뷰에서
    그림이 깨진다** — 그 빌드는 JS·CSS만 인라인한다(음원과 같은 자리)
  - ⚠️ **어두운 화면에서는 흰 판을 깐다** (`index.css`의 `.zion-logo`). 래스터라 팔레트처럼
    안 뒤집혀 남색 선이 묻힌다(대비 **1.45** 실측). 선체 안쪽도 원래 흰 면이라 투명이어서
    필터로 밝히는 것으로는 해결되지 않는다
  - ⚠️ **PowerShell에 한글이 든 스크립트를 넘기지 않는다** — 변환 스크립트를 한글 주석째
    넘겼다가 ANSI로 읽혀 통째로 깨졌다. ASCII로만 쓰거나 Node로 옮긴다
- **사이드바 레일은 lg 이상 전용**이다. 폭 정본은 Layout의 CSS 변수 `--sidebar-w`(272/68) —
  종전 272px 이중 하드코딩을 이것으로 모았다.
  - ⚠️ **고정을 풀 때 `hovering`을 함께 꺼야 한다** (2026-08-13에 잡았다). 안 그러면 마우스가
    아직 사이드바 위에 있어 `expanded`가 참으로 남는다 — **본문 여백(`--sidebar-w`)만 68px로
    줄고 패널은 272px 그대로**여서 「핀을 눌렀는데 안 접힌다」로 보인다. 본문만 움직이니
    오히려 고장으로 읽힌다. 접은 뒤 마우스를 다시 올리면 종전대로 잠시 펼쳐진다
  ⚠️ **relatedTarget 없는 blur는 무시한다** — 펼쳐지는 순간 레일 DOM이 사라지며 포커스가
  body로 떨어지는데, 그걸 「나감」으로 읽으면 펼침·접힘이 진동한다(실측으로 잡은 결함)
- **자료실 구획이 없어졌다** (2026-08-13). 「강사 도우미 자료실 / 외부 자료실」 두 갈래가
  사라지고 **폴더 한 줄기**가 됐다 — 「외부 자료실」은 애초에 없던 개념이고, 외부 매체는
  자료에 붙는 참고 링크였다.
  ⚠️ **`section` 필드는 지우지 않았다.** 저장된 자료가 값을 갖고 있어 지우면 후방
  마이그레이션이다 — **읽는 곳이 없을 뿐 남아 있다**(`division`과 같은 취급)
  - 자료 목록·상세·등록은 **`src/components/FolderLibrary.tsx` 한 벌**을 자료실과 강의
    도우미가 나눠 쓴다. 새 화면에서 폴더 자료를 열어야 하면 **이걸 props로 부른다**
  - 옛 링크(`?section=…`·`?tab=…`)는 그대로 동작한다 — 북마크가 죽지 않게 계속 받는다
- **강의 녹취 정리도 규칙 기반이다** (`/digest`, 2026-08-13). 자막(.srt/.vtt)이나 받아쓴 글을
  넣으면 구간으로 자르고 **성구 · 교안 매칭 · 주제어 · 고른 문장**을 낸다. 비용 0 · 저장 안 함.
  - ⚠️ **받아쓰기(STT)는 하지 않는다.** 브라우저에서 돌리려면 모델 150MB에 1시간 강의 수십 분이라
    휴대전화에서 못 쓴다. **밖에서 무료 도구로 받아쓴 뒤 넣는** 구조다
  - ⚠️ **유머는 규칙으로 못 잡는다** — 웃음은 글에 안 남는다. 외부 AI 프롬프트로 넘긴다
  - ⚠️ **`MarkdownLite`로 녹취록을 그리지 않는다.** `stripArtifacts`가 **연속 2자 이상 한자를
    지우고** `- `를 불릿으로 바꾼다 — 표시 단계에서 원문이 변형된다. `whitespace-pre-wrap`을 쓴다
  - ⚠️ **한국 `.srt`는 EUC-KR이 흔하다.** `file.text()`는 UTF-8로만 읽어 통째로 깨진다 —
    `readSubtitleFile`이 대체 문자 비율을 보고 자동으로 다시 읽는다
  - 성구 인식은 **`src/lib/bible-refs.ts` 한 곳**이다. `AdvicePanel`의 옛 `VERSE_LINE`도
    여기로 흡수했다 — 종전에는 두 글자 책 이름까지만 받아 `고전10:13`을 놓쳤다
- **성향 분석의 관찰문→프롬프트 구조는 2026-08-13에 걷어냈다** (같은 날 만들고 같은 날 없앴다).
  `/tendency`는 이제 수강생을 고르면 **기록된 에니어그램 유형의 원문**(`enneagram-guides.ts`)을
  `/enneagram`과 같은 모양(`Enneagram.tsx`의 `GuideItems`를 내보내 공유)으로 그대로 보여 줄
  뿐이다. 관찰문 입력 · 규칙 엔진 매칭 · 동의 게이트 · 외부 GPT 프롬프트는 전부 뺐고, 그
  엔진이던 `src/lib/advice-engine.ts` · `src/components/AdvicePanel.tsx` ·
  `src/lib/advice-corpus.ts`도 쓰는 곳이 이 화면뿐이라 함께 지웠다(되살릴 때는 git 이력에서
  꺼낸다). 불변식 4는 그대로 지킨다 — 유형을 추정하지 않고 기록된 값만 보여 준다
- **다크 모드는 화면 파일을 한 곳도 안 고치고 넣었다** (2026-08-11).
  `src/index.css`의 `[data-theme="dark"]`가 팔레트 변수를 갈아 끼운다.
  ⚠️ **진한 배경으로 쓸 수 있는 클래스가 정해져 있다** — `CLAUDE.md` 화면 규칙의 목록을 본다.
  목록 밖 단계를 배경에 쓰면 다크에서 하얗게 뜬다
- **밝은·어두운 두 화면 모두 AA(4.5)를 통과한다** (2026-08-11 실측 — 22개 라우트 + 팝업·드로어).
  ⚠️ `text-zion-300`은 **글자일 때만** 따로 어두운 값(`--text-zion-muted`)으로 바뀐다 —
  같은 단계를 `border-zion-300`·`bg-zion-300`으로 쓰면 종전대로 옅다. 일부러 그렇게 뒀다
  (비활성 버튼은 흐릿해야 눌리지 않는 것으로 읽힌다)
- 🔓 **외부 AI를 쓸 수 있게 됐다** (2026-08-11 리드 변경). 종전 「수강생 개인정보를 AI
  입력에 넣지 않음」이 **「동의받고 비식별화한 정보만 보낸다」**로 바뀌었다 (불변식 4).
  텔레그램 봇 연동이 이 한 줄에 막혀 있었다.
  **보내는 쪽은 `src/lib/privacy.ts`의 `prepareForAI(글, 동의상태)`만 쓰면 된다** —
  동의 확인과 비식별화가 그 안에서 끝난다. 동의를 모르면 함수가 막는다(기본값이 「안 보냄」).
  - ⚠️ **정규식은 사람 이름을 못 잡는다.** 「김민수 형제님」은 걸리지만 「민수가 요즘」은
    안 걸린다. 그래서 **서버에서 같은 검사를 다시** 해야 하고, 브라우저 검사만 믿으면 안 된다
  - ⚠️ **키를 브라우저에 두지 않는다** — 우리 서버를 거쳐 부른다 (`VITE_ASK_API_PATH`)
  - 반응 분류(`reaction-classify.ts`)의 낱말 규칙은 **폴백으로 남긴다** — 동의가 없거나
    API가 죽어도 화면이 멈추면 안 된다
- **카카오맵 키는 `.env`에 들어 있다** (앱 「시온 아크」 ID 1540603, 2026-08-11 등록).
  `.env`는 git에 안 올라가므로 **새 기기에서 받으면 지도가 안 뜬다** — 콘솔에서 키를 복사해
  `VITE_KAKAO_MAP_KEY`에 넣는다. 등록된 도메인은 `qtg002505.github.io` ·
  `localhost:5173` · `localhost:4173`이다.
  ⚠️ **개발 서버가 다른 포트로 뜨면 그 포트도 콘솔에 더해야 한다** (autoPort로 바뀔 수 있다).
  ⚠️ **제품 설정 > 카카오맵을 켜야 한다** — 키와 도메인이 맞아도 이게 꺼져 있으면 **503**이 온다
- ⚠️ **화면 위에 띄우는 것은 `src/components/Portal`로 감싼다** (2026-08-13) —
  모달 · 팝오버 · 시트 전부. `fixed inset-0`에 `z-50`을 줘도 **`main` 안에 있으면 헤더에 눌린다**
  (`main`이 `view-transition-name` 때문에 쌓임 맥락을 만든다). 실제로 수강생 상세 팝업 머리가
  잘렸다. **숫자를 올려도 안 고쳐진다** — 맥락 밖으로 꺼내야 한다.
  지금 열 곳이 이미 `Portal`을 쓴다. **새 모달도 처음부터 감싼다**
- **링크는 `src/components/TransitionLink`에서 가져온다** (2026-08-11).
  `react-router-dom`에서 직접 가져오면 화면 전환 효과가 조용히 빠진다 —
  라우터의 `viewTransition`은 데이터 라우터 전용인데 이 앱은 `<BrowserRouter>`다
- **데이터는 브라우저 안에만 있다** (localStorage). 같은 브라우저에서 재로그인하면
  그대로 남지만 **다른 기기·브라우저에서는 안 보인다** — 백엔드가 붙어야 풀린다
- **음악은 팀 공유 링크(Pages)에서만 들린다.** 아티팩트 프리뷰는 단일 HTML이라 음원을
  넣으면 16MB 제한을 넘어 일부러 뺐다
- **시범 로그인은 인증이 아니다.** 브라우저 콘솔에서 `localStorage`를 고치면 아무 역할이나
  될 수 있다 — 실데이터 투입 전 정리가 선행이다
- **2026-08-18 리드 피드백을 대량 반영했다** — 자세한 것은 `HISTORY.md`의 「(5)」 항목이다.
  지금 상태만 적으면:
  - 수강생에 **혈액형·종교·신앙년수**가 있고, **특이사항은 여러 건**을 쌓는다(고치기·지우기)
  - **「수강생 현황」 안에 「AI 성장 추천」 탭**이 있다. 수강생 상세의 AI 분석 카드는 **그리로
    옮겼다** — 계산은 `lib/growth-analysis.ts` 한 곳이다
  - 기수 요약 맨 위가 **총회 점검(1주차·4주차 대면 3회)**이고, 그 아래 **수강생 구성**(도넛),
    **진도별 보강 포함 현황**(최신이 왼쪽·드래그)이 온다
  - 출석 격자는 **머리가 고정**되고 **분반(전도사) 탭**이 있으며 **손이 필요한 분이 위**다
  - 「지금 우리 기수는?」에 **수강생 × 항목 한눈 표**가 강점·약점보다 먼저 온다
  - 자료는 **지파 · 개강 갈래(자장부/청년) · 해시태그**로 찾는다
  - 성향 분석은 **분반 → 수강생 2단**이고 **MBTI 네 축 안내**가 붙는다
  - ⚠️ **취소된 것 둘**: 「난해 질문 파트」 · 「단계표 달성률 전도사별 집계」. 만들지 않았다
  - ⚠️ **확인 대기**: 사명자 현황의 「눌렀을 때 초·중·고 별로 상세히」 — 취소된 집계와 겹칠
    소지가 있어 지금은 **명단까지만** 펼친다
- **갈래 전환기(세그먼트 탭)를 부품 하나로 모았다** (2026-08-18 리드 지시 — 「전환 버튼 더
  가독성 있게」). `components/SegmentedTabs.tsx` 한 벌을 **열세 곳**이 나눠 쓴다.
  - 고른 항목이 **진한 남색 + 흰 글자**다. 종전(옅은 트랙 위 흰 알약)은 면 대비 **1.1**이라
    흰 카드 안에서 경계가 사라졌다 — 지금은 **8.3**
  - ⚠️ **어두운 화면 함정 셋을 실측으로 잡았다.** ① 안 고른 글자를 `text-zion-600`으로 두면
    트랙과 함께 어두워져 **1.96**이다(→ `text-ink-soft`, 5.51) ② `hover:bg-white/70`은
    **다크 되돌리기 목록에 없어** 흰 띠로 뜬다(→ `hover:bg-zion-200`) ③ 고른 항목의 면 대비가
    **1.72**까지 떨어져 `index.css`가 안쪽 테두리 한 줄을 얹는다(`[data-segmented-on]`)
  - ⚠️ **대비를 잴 때는 색 전환을 꺼야 한다** — `transition`이 computed value를 중간값에
    묶어 둔다. `getComputedStyle`이 `oklab(...)`으로 돌려주면 그 신호다(이걸 모르고 두 번
    잘못 읽었다). `*{transition:none}`을 잠깐 주입하고 잰다
- **키보드 초점 링이 생겼다** (2026-08-18). 종전에는 초점 규칙이 **한 줄도 없어** 브라우저
  기본 검은 사각 테두리가 나왔다 — 둥근 단추 위에 각진 테두리가 겹쳤다.
  `index.css` 한 곳에서 브랜드 남색 링을 주고 진한 면 위에서는 흰 링으로 바꾼다
- **마이페이지 「내 일정」은 월간이 기본이다** (2026-08-18 리드 지시). 주간은 그 달 안에서
  좁혀 보는 눈금이 됐다. 저장소는 하나이므로 어느 눈금에서 넣어도 양쪽에 다 보인다
- **카드 테두리를 `zion-200`으로 한 단계 올렸다** — 바탕과 카드면의 대비가 **1.06**이라
  경계를 만드는 것이 테두리뿐인데 그마저 옅어 카드가 배경에 묻혀 있었다
- **첫 화면 번들을 갈랐다** (2026-08-18) — 3.17MB/gzip 1.06MB → **792KB/gzip 369KB**.
  화면 코드는 라우트마다(`App.tsx`의 `lazy`), 원문 자료는 쓸 때 받아 온다.
  - **셸에 매달린 것이 진짜 원인이었다** — `search.ts`가 시리즈·어록·교안 원문을 정적으로
    안고 있었고 그 파일을 검색창이 불렀다. `DailyQuote`도 한 줄 보여 주자고 어록 468KB를
    지고 있었다. **라우트만 갈라서는 첫 화면이 안 줄던 자리다**
  - ⚠️ **새 화면을 셸(`shell/**`)에 붙일 때는 무엇을 끌어오는지 본다** — 셸에 붙은 것은
    모든 화면이 함께 진다. 자료를 봐야 하면 `search.ts`처럼 **쓸 때 동적으로** 가져온다
  - ⚠️ **팀 공유 프리뷰는 이 가르기를 되돌려 빌드한다**(`vite.config.ts`의 `preview` 모드가
    청크를 합친다). 단일 HTML은 외부 조각을 못 받아 와 갈라 두면 첫 화면 말고는 안 뜬다
  - 남은 몫: `series-content` 청크가 1.4MB다 — 시리즈 화면을 열 때만 받지만 그 화면은 무겁다
- **자동 테스트 63건이 생겼다** (2026-08-18 · `npm.cmd test`). 순수 로직만 덮는다 —
  기수 일정 산수(`cohort-calendar` 16) · 권한 판정(`permissions` 17) ·
  개인정보 거르기(`privacy` 18) · 띄어쓰기 무시 매칭(`text-match` 12).
  - 문서에 적어 둔 검산(105회 · 주차 라벨 · 일요일 규칙)을 **사람이 다시 세지 않아도 되게** 옮겼다
  - ⚠️ **화면 렌더 테스트는 아직 없다.** 도움됨 중복 클릭·타인 글 수정 차단처럼 화면을
    거쳐야 보이는 것은 여전히 손으로 확인한다
  - ⚠️ 설정은 `vitest.config.ts`에 따로 둔다 — 운영 빌드가 테스트 도구에 매이지 않게 갈랐다

## 팀 운영

- 파트 분담은 `TEAMWORK.md`. 팀원은 `/work`(이슈 작업)와 `/push`(올리기) 스킬을 쓴다
- ⚠️ **리드는 `main`에 직접 push할 수 있다**(보호 규칙 bypass). 팀원 PR과 겹치지 않게
  파트 파일을 건드릴 때는 담당자에게 알린다
- ⚠️ **승인 필수는 꺼져 있다** — 파트 파일만 고친 PR은 팀원이 직접 머지한다.
  전부 리드가 보려면 GitHub → Settings → Branches → `main` → "Require approvals: 1"

### 브랜치를 합칠 때 — 2026-08-13에 배운 것

- ⚠️ **팀원에게 「최신 main에서 브랜치를 새로 따라」고 먼저 알린다.** 파트 B 브랜치가
  나흘 묵는 사이 main이 **73개 파일 8,888줄** 바뀌어, 단순 충돌이 아니라 **설계 방향이
  반대**가 됐다(상세를 팝업으로 여느냐 페이지로 여느냐). 오래 묵을수록 합치는 비용이
  기하급수로 는다
- ⚠️ **합칠지 물을 때는 작업 요약의 항목을 하나씩 짚는다.** 「팝업이냐 페이지냐」 한 축으로만
  물었더니 같은 브랜치의 다른 개선(**목록 1단화**)이 조용히 사라졌고, 리드가 화면을 보고서야
  발견했다. 축 하나로 묶어 물으면 나머지가 묻힌다
- **팀원 코드는 대비를 안 재고 올 수 있다.** 파트 B의 오행 뱃지가 밝은 면에 흰 글자라
  대비 2.13이었다. 머지 뒤 **밝은·어두운 두 화면에서 다시 잰다**
