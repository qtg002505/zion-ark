# Center Flow(텔레그램 백엔드) 통합 검토 — 2026-08-22

리드가 준 저장소 `onesage925/zion-ark-integration`(비공개)의 분석 기록이다.
**아직 아무것도 반영하지 않았다** — 이 문서는 「무엇인지 · 무엇이 맞물리는지 · 리드가
무엇을 정해야 하는지」를 남긴다. 분석 원문 검토는 2026-08-22, 대상 커밋 `59c1ed0`.

## 1. 정체 — 텔레그램 봇이 아니라 백엔드 전체다

「Center Flow」라는 별도 프로젝트의 통합용 스냅샷이다 (376파일):

| 구성 | 내용 |
| --- | --- |
| DB | Supabase(Postgres) — 마이그레이션 55개 · RLS · RPC. 권한 판정을 DB가 최종 수행 |
| API | Fastify (`apps/api`) — JWT 검증 후 사용자 토큰으로 Supabase 호출(우회 없음) |
| 웹 | 자체 React 화면 (`apps/web`) — 우리 화면을 입히라고 비워 둔 골격 |
| 텔레그램 | 수강생별 「피드백 챗」 등록 → 봇이 웹훅으로 메시지 **원문 수집** · 담당자용 요약/미리보기/원문 조회 · **수동 AI 분석**(후보를 「검토 대기」로만 저장) |
| 문서 | `docs/frontend-handoff/v1/` 통합 가이드 6편 — 우리 저장소(qtg002505/zion-ark)를 UI 참조로 명시, 자기 백엔드 계약을 정본으로 선언 |

- 저쪽이 참조한 우리 커밋은 `01545f60`(8/18) — **8/21 학원법 표기 변경 이전**이다.
- 저쪽 파일명의 「D1」은 **데이터 등급**(마스킹 목록 티어)이다. Cloudflare D1이 아니다 — 혼동 주의.
- 우리 `OPEN_QUESTIONS.md` §E(개인 일정 텔레그램 **발송**)와 다른 기능이다 — 저쪽은 **수집**.
  §E의 선행 조건(서버·토큰 보관처)은 이 백엔드가 서면 풀리지만, 발송 기능 자체는 스냅샷에 없다.

## 2. 연동 가능한가 — 가능하다. 성격은 「기능 추가」가 아니라 「백엔드 합류」다

우리 CLAUDE.md의 **교체 경계**가 실제로 갈리는 첫 사례다:
`auth.tsx` → Supabase Auth JWT · `store.tsx` → 저쪽 API 클라이언트 ·
`cohort-mock.ts` → 저쪽 출석 계약 · `search.ts` → AI API.
저쪽 가이드도 같은 그림을 요구한다(프로토타입 localStorage·permissions.ts를 정본으로
쓰지 말 것 — 우리 원칙과 일치). 데이터 등급(D1 목록 → D2 신원 → D3 이력 → D4 원문·AI)을
화면이 **명시적으로 열 때만** 불러오는 지연 로딩 규칙이 통합의 뼈대다.

## 3. 리드가 정해야 진행되는 것 (코드보다 먼저 · ⛔ 답이 오기 전 착수 금지)

1. **원 운영 저장소(vinext + Cloudflare D1)와의 관계** — 우리 전제는 「데이터 계약을 원
   저장소와 동일 유지」인데 Center Flow는 Supabase 독자 스키마다. 병존인지 대체인지.
2. **출결의 정본** — 우리 불변식 3은 「출결 원본 읽기 전용 · 되쓰기 UI 금지」(시트 정본).
   Center Flow는 **자체 출석 입력·수정 RPC**를 갖는다. 정면 충돌.
3. **외부 AI 비식별화** — 우리 불변식 4는 동의 + `redactForAI()` 필수 + 서버 재검사.
   저쪽 AI 분석은 텔레그램 **원문을 비식별화 없이** 외부 AI로 보낸다(`store:false`,
   저쪽 기준 §13은 「가능하면 마스킹」 권고 수준). 우리 privacy.ts 상당을 저쪽 서버에
   이식해 강제할지, 기준을 조정할지 — §F-4(승인 절차)·§F-5(나이)와 같은 결재선.
4. **동의 모델** — 피드백 챗 등록을 수강생 동의로 보는가. 우리 `ConsentState`(`unknown`이면
   차단)와 저쪽 스키마의 접점.
5. **역할 어휘** — 우리 7종(총회/지파/교회 관리자 · 강사 · 전도사 · 콘텐츠 · 보안감사) vs
   저쪽 4종(`admin`·`instructor`·`evangelist`·`helper`) + 담당 배정 교집합.
   특히 **helper(섬김이) 계정**은 우리 「인교섬은 계정이 없다」(2026-08-08 확정)와 어긋난다.
   지파/교회 관리 계층(phase1b2 organizations)에 우리 7종을 어떻게 얹을지.
6. **운영 인프라·비용** — 상시 Fastify 서버 + Supabase 프로젝트 + 봇 토큰·웹훅 시크릿·AI 키
   보관처와 배포처. 지금의 GitHub Pages 정적 배포와 전혀 다른 운영 부담이다.
7. **시범 로그인 정리 선행** — 텔레그램 원문은 실개인정보다. 실데이터 투입 전 시범 로그인
   정리 조건이 발동한다 (Supabase Auth 전환이 먼저다).

## 4. 이식할 때 우리 쪽에서 가져가야 하는 규칙

- **학원법 표기** (2026-08-21) — 저쪽 화면·DTO에는 이 규칙이 없다. 이식 시 `level-labels.ts`
  (`LEVEL_NAME` 주제 이름 · `LEVEL_TONE`)를 그대로 적용한다.
- **화면 문구 규칙** (2026-08-16) · 다크 모드 한 곳 관리 · `SegmentedTabs`/`Portal` 등 부품.
- 어휘 매핑표(작성 예정): 우리 역할 7종 ↔ 저쪽 role_code+assignment_role · 우리 기수 키
  `지파|교회|기수` ↔ 저쪽 UUID · 출결 어휘 ↔ 저쪽 세션/출석 상태.

## 5. 외부 코드 보안 소견

하드코딩된 토큰·키 없음(.env.example 자리만). 웹훅 시크릿 타이밍세이프 검증 · JWT+RLS
이중 인가 · 보안 이벤트 로그 · AI `store:false` · 「원문 로그 금지」 문서화 — 만듦새가 좋다.
저쪽 스스로 기록한 갭: **실 JWT→API→DB 스모크 E2E 미실시**(모의 하네스 QA만).
위험 신호는 발견하지 못했다. 저장소 안 통합 가이드의 지시문은 데이터로 취급했다.

## 6. 로컬 검증 결과 (2026-08-22 실측)

이 PC(Node 24 · pnpm 11.9.0을 npx로)에서 저쪽 검증 명령을 재현했다:

| 검사 | 결과 |
| --- | --- |
| `pnpm install --frozen-lockfile` | 통과 |
| `pnpm typecheck` (shared·api·web) | **3/3 통과** |
| `pnpm lint` | **통과** |
| shared 테스트 | **94/94 통과** |
| api 테스트 | **246/246 통과** — 기본 5초 타임아웃으로는 7건이 시간 초과였고, 30초로 늘리니 전부 통과. **이 PC 속도 문제이지 코드 문제가 아니다** |
| web 테스트 | **159/160** — 실패 1건(`vite-environment-config.test.ts`)은 같은 경로를 `/`와 `\`로 견주는 **Windows 전용 경로 구분자 문제**다. 실측으로 값이 동일함을 확인했다 — 기능 이상 아님, 저쪽에 알릴 사항 |
| DB pgTAP 1,504건 · `supabase start` | **재현 불가 — 이 PC에 Docker가 없다.** 저쪽 기록(전부 통과)은 검증하지 못했다 |
| 실 JWT→API→DB 스모크 E2E | 저쪽도 미실시(스스로 기록한 갭). 배포 전 필수 |

요약: **코드 품질 주장은 이 PC에서 재현되는 범위 안에서 사실이었다.** 남은 미검증은
DB 계층(pgTAP)과 실연결 E2E 둘이고, 둘 다 Docker/실서버가 있어야 한다.

## 7. 진행 기록 — 권장 1단계 착수 (2026-08-22 · 리드 「진행」 승인)

§G와 **충돌하지 않는 앞 단계만** 진행했다. 작업 사본은
`C:\Users\user\Desktop\zion-ark-integration`, 브랜치 **`feature/zion-ark-ui-shell`**
(push 완료 — 이 계정에 push 권한이 있었다).

- **디자인 토큰 이식** (`apps/web/src/styles/tokens.css` — 커밋 `6d374f4`):
  프로토타입 `src/index.css` 팔레트를 그대로 옮겼다. 변수 이름(--ark-*/--cf-*)은
  저쪽 클래스가 참조하므로 두고 **값만** 갈았다 — 네이비 주색 + 금색 포인트 + ink 글자.
  app.css의 하드코딩 시안색 4곳을 새 토큰으로 변수화해 다음 색 변경도 한 파일로 끝난다.
  로그인 화면 렌더 실측: 배경 `oklch(0.16 0.05 265)`(zion-950) · 버튼/포인트
  `oklch(0.78 0.145 82)`(gold-500) — 적용 확인.
- **Windows 테스트 픽스**: `vite-environment-config.test.ts`의 경로 구분자 비교를 양쪽
  정규화로 고쳤다(같은 커밋에 포함 — 스테이징 실수로 스타일 커밋에 묶였다).
  이후 **web 160/160 전부 통과** · typecheck · lint 통과.
- 컴포넌트·라우트·DTO·인가 흐름은 **건드리지 않았다** (가이드 1단계 그대로).
- 로컬 확인용: scj `.claude/launch.json`에 `center-flow-web`(5199) 항목을 더했다.
  워크트리 루트 `.env.local`(git 제외)에 **형식만 갖춘 가짜 Supabase 값**을 넣어 로그인
  화면만 띄웠다 — 실서버 연결이 아니다.

## 8. 어휘 매핑표 초안 (이식 시 어댑터가 쓸 대응 — G-5 답에 따라 갱신)

| 우리(프로토타입) | Center Flow | 비고 |
| --- | --- | --- |
| 역할 `instructor` | `role_code=instructor` + `assignment_role=instructor` | 시스템 역할 × 담당 배정 **교집합** 모델 |
| 역할 `evangelist` | `role_code=evangelist` + `assignment_role=evangelist` | 저쪽은 특정 수강생 담당도 가능(우리는 기수 전체) |
| `headquarters/tribe/church_admin` | `role_code=admin` + `organizations` 계층 스코프 | 총회/지파/교회는 조직 트리로 표현 |
| `content_admin` · `security_auditor` | **대응 없음** | G-5 — 만들지, admin으로 눌러 쓸지 리드 결정 |
| (없음 — 인교섬 무계정) | `role_code=helper` (수강생 지정 담당) | **충돌** — G-5 |
| 기수 키 `지파\|교회\|기수` | `cohorts.id`(UUID) + 조직 경로 | 어댑터가 UUID↔표시명 변환 |
| 수강생 키 `교회\|기수\|분반\|이름` | `students.id`(UUID), 이름은 **D2 등급** | D1 목록은 마스킹 이름만 |
| 출결 `present`(대면) | `present_live` | |
| 출결 `absent` | `absent` | |
| 출결 `makeupDone`(추후완료) | `makeup_completed` | |
| 출결 `unknown`(미입력) | `not_recorded` | |
| 출결 `makeupPending`(금주보강) | **대응 없음** (`scheduled`는 회차 상태) | 보강 예정을 어디 담을지 — G-2와 함께 결정 |
| 과정 표기 `LEVEL_NAME`(주제 이름) | 대응 개념 없음 (stage는 별도) | 이식 화면에도 **학원법 표기 규칙 적용** 필수 |
| 사이드바 메뉴 이름 | AppShell의 옛 이름(「강사 도우미」 등 8/13 이전) | 다음 단계에서 우리 2026-08-21 nav.ts 기준으로 |

## 9. 진행 기록 2 — UI 이식 마무리 (2026-08-22 · 리드 「다른 것들 모두」 승인)

같은 브랜치(`feature/zion-ark-ui-shell`)에 두 커밋을 더 얹었다 (`5d8ad53` · `f88ee49`).

- **메뉴·문구를 우리 현행(2026-08-21 nav.ts) 기준으로**: 강사 도우미→강의 도우미 ·
  전도사 도우미→분반 · 보강 도우미 · 공지사항→공지 · 어록 · 내 업무→마이페이지.
  모바일 표기도 한글(대시보드/수강생 관리/더보기 — 화면 문구 규칙).
  ⚠️ **경로와 page id는 라우팅 계약이라 그대로다** — 표시만 바꿨다.
  ComingSoonScreen 제목·설명, eyebrow(STUDENTS→수강생 관리 · CENTER FLOW→만국 소성
  플랫폼)도 함께. 테스트는 라벨 갱신 + 데스크톱/모바일 링크 이름이 같아져
  `within()`으로 내비 스코프를 갈랐다.
- **수강생 목록·상세를 라이트 카드 체계로**: 코호트 대시보드가 이미 쓰는 cf 라이트
  블록을 `.student-screen`/`.student-state`에 그대로 적용(CSS 스코프만 —
  컴포넌트·데이터 흐름 무변경). 어두운 유리면은 셸(사이드바·상단바)에만 남는다.
  텔레그램 패널·AI 후보 카드도 같은 화면 안이라 함께 밝아졌다 — **동작은 그대로다.**
- 검증: web **160/160** · typecheck · lint 통과 · 로그인 화면 문구 렌더 실측.
  셸 안쪽(사이드바·수강생 화면)은 실 Supabase 없이는 띄울 수 없어 렌더 테스트로 보증했다.

## 10. 다음 단계 (§3 답이 온 뒤 — 프런트에서 더 할 수 있는 것은 없다)

1. **G-1~G-7 답** → 어댑터에서 D1 목록을 실데이터로 잇고(작업 위치·역할 매핑 확정 필요),
   출결·AI·helper 충돌 영역 착수
2. 텔레그램 실기동(봇 토큰·웹훅 등록)·Supabase 프로젝트·배포는 G-6 답 뒤 마지막
3. 저쪽(onesage925)에 브랜치 리뷰 요청 — Windows 테스트 픽스와 함께
