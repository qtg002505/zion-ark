# 팀원 온보딩 — 처음부터 PR까지 순서대로

이 문서 하나만 따라 하면 된다. 각 단계에 **[사람]**(직접 손으로) / **[터미널]**(명령 복붙) /
**[AI]**(Claude Code에게 시키기) 표시를 달았다. 자기 파트가 무엇인지는
[TEAMWORK.md](TEAMWORK.md) 분담표에서 먼저 확인한다.

---

## 1부. 최초 1회 준비

### 1. GitHub 계정과 초대 [사람]

1. https://github.com 가입 (이미 있으면 생략)
2. 리드에게 GitHub 아이디를 알려 준다
3. 메일(또는 github.com 알림)로 온 **저장소 초대를 수락**한다 — 수락 전에는 아무것도 안 보인다

### 2. 프로그램 설치 [사람]

| 프로그램 | 받는 곳 | 확인 명령 |
|---|---|---|
| Git | https://git-scm.com/download/win | `git --version` |
| Node.js 22 LTS | https://nodejs.org | `node --version` |
| Claude Code | https://claude.com/claude-code | `claude --version` |

설치 후 터미널(PowerShell)을 **새로 열어** 확인 명령이 버전을 찍는지 본다.

Git 최초 설정 (본인 이름·메일로):

```
git config --global user.name "이름"
git config --global user.email "메일주소"
```

### 3. 저장소 받기 [터미널]

```
cd Desktop
git clone https://github.com/qtg002505/zion-ark.git
cd zion-ark
npm install
```

### 4. 대시보드 띄우기 [터미널]

```
npm run dev
```

브라우저에서 `http://localhost:5173` → 시범 로그인에서 **자기 파트와 관련된 역할**을 골라
들어간다. **이 화면을 옆에 켜 두고 작업한다** — 고친 것이 저장 즉시 여기 반영된다.

여기까지 되면 준비 끝. 리드에게 "세팅 완료" 알린다.

---

## 2부. 작업 사이클 (이슈 하나마다 반복)

### 5. 지시 확인 [사람]

GitHub 저장소 → **Issues** → 자기에게 지정된(Assignee) 이슈를 연다.
**완료 기준**과 **건드리지 않는 파일**을 읽는다. 모호하면 이슈에 코멘트로 묻는다 —
카톡으로 묻지 않는다 (기록이 남아야 리드가 취합할 때 헤매지 않는다).

### 6. 최신으로 맞추고 브랜치 만들기 [터미널]

```
git switch main
git pull origin main
git switch -c part-a/이슈요약
```

`part-a`는 자기 파트 글자(a~e)로. **이걸 건너뛰고 옛 코드에서 시작하는 것이
충돌 원인 1위다.** 작업 시작 전 반드시 pull.

### 7. AI에게 시키기 [AI]

저장소 폴더에서 `claude`를 실행하고, 아래를 채워 붙여넣는다.

**요령은 하나다 — 이슈를 요약하지 말고 통째로 붙여넣는다.** 이슈 양식에는 담당 파일 ·
건드리지 않는 파일 · 그 파트의 불변식이 이미 들어 있다. 「할 일」만 옮기면 AI는 경계를
모른 채 시작하고, 공유 파일을 건드려 **머지 버튼이 잠긴 PR**이 된다.

```
나는 시온 아크 파트 (A~E) 담당입니다.

시작 전에 docs/HANDOFF.md 와 docs/TEAMWORK.md 를 읽어 주세요.
main에서 최신을 받고 part-(a~e)/(요약) 브랜치를 새로 만든 뒤 작업합니다.

--- 아래는 GitHub 이슈 #(번호) 전문입니다 ---
(이슈 페이지를 통째로 복사해 붙여넣기 — 배경·할 일·완료 기준·담당 파일·
 건드리지 않는 파일·불변식까지 전부)
---

지켜 주세요:
- 「담당 파일」 안에서만 고칩니다. 「건드리지 않는 파일」은 읽기만 하고, 바꿔야 하면
  코드 대신 "공유 파일 변경 요청" 목록으로 정리해 주세요
- 완료 기준을 하나 채울 때마다 커밋해 주세요. 한 번에 몰아서 커밋하지 않습니다
- 이슈에 없는 기능은 임의로 추가하지 마세요. 필요해 보이면 먼저 물어봐 주세요
- 끝나면 npm run typecheck 와 npm run build 를 돌리고, 미리보기에서 직접 열어
  콘솔 오류 0 과 375px 폭 가로 넘침 0 을 확인해 주세요
- 확인까지 끝나면 push하고, .github/pull_request_template.md 양식에 맞춰
  PR 본문 초안을 써 주세요 (Closes #번호 포함)
```

**작업 도중 쓰는 짧은 지시 셋** — 한 덩어리가 끝날 때마다 올리기 위한 것이다.

| 상황 | 이렇게 말한다 |
|---|---|
| 한 덩어리 끝남 | `여기까지 커밋하고 푸시해 주세요. PR은 아직 만들지 않습니다` |
| 공유 파일이 필요해짐 | `그 파일은 고치지 말고, 무엇을 왜 바꿔야 하는지 "공유 파일 변경 요청"으로 정리해 주세요` |
| 이슈가 너무 큼 | `이슈를 두세 덩어리로 나눈다면 어디서 끊는 게 좋을까요? 첫 덩어리만 먼저 하겠습니다` |

저장소의 `CLAUDE.md`(불변식·데이터 계약·화면 규칙)와 `.claude/skills/`는 AI가 자동으로 읽는다 —
지시문에 옮겨 적을 필요가 없다. AI가 고치는 동안 **4번에서 켜 둔 대시보드를 새로고침하며
눈으로 확인**한다.

> **왜 작게 자주 올리나**: 이슈 하나에 일주일치를 몰아 오면 리뷰도 충돌도 그만큼 커진다.
> **완료 기준을 채우면 그 자리에서 올린다** — 이슈 하나 = PR 하나가 기본이고, 이슈가
> 크다 싶으면 리드에게 쪼개 달라고 요청한다.

### 8. 사람이 직접 확인 [사람]

AI 통과 보고만 믿지 말고 직접 본다:

- 대시보드에서 완료 기준대로 동작하는지
- 브라우저 F12 → Console 탭에 빨간 오류가 없는지
- F12 → 기기 아이콘(반응형 모드) → 폭 375로 좁혀 가로 스크롤이 안 생기는지

### 9. 올리기 [AI 또는 터미널]

7번 지시문대로면 AI가 검증까지 마치고 이미 push해 둔 상태다. 직접 하려면:

```
git add -A
git commit -m "feat: 무엇을 왜 바꿨는지 한 줄"
git push -u origin part-a/이슈요약
```

### 10. PR 만들기 [사람]

1. push 직후 GitHub 저장소에 뜨는 노란 배너 **"Compare & pull request"** 클릭
   (배너가 없으면 Pull requests → New pull request → 자기 브랜치 선택)
2. 본문 양식이 자동으로 뜬다 — 7번에서 AI가 써 준 **PR 본문 초안**을 붙여넣고
   체크리스트에 체크한다
3. `Closes #이슈번호`를 반드시 적는다 — 머지되면 이슈가 자동으로 닫힌다
4. **공유 파일 변경 요청**이 있으면 그 칸에 적는다 — 리드가 대신 반영한다
5. Create pull request 클릭

### 10-1. 머지하기 [사람] — 자기 파트 파일만 고쳤다면 직접

**내 파트 파일만 고쳤으면 승인을 기다리지 않는다.** PR 화면 아래 초록색
**"Merge pull request"** 버튼을 눌러 스스로 반영한다. 이어서 **Confirm merge**까지 누르면 끝.

버튼이 회색이고 *"Review required"* 라고 뜨면 → **공유 파일을 건드린 것이다.**
어느 파일인지 PR의 "Files changed" 탭에서 확인하고, 그 수정은 되돌린 뒤
본문의 "공유 파일 변경 요청"에만 적는다. 리드가 대신 반영한다.
(수정 요청 코멘트가 달리면 같은 브랜치에서 고쳐 다시 push하면 PR에 자동 반영된다.)

### 11. 머지된 뒤 [터미널]

머지하면 몇 분 안에 팀 공유 링크(GitHub Pages)에 반영된다. 다음 작업은
**반드시 6번부터 다시** — `git switch main` → `git pull` → 새 브랜치.

---

## 하지 말 것 (전 파트 공통)

- 공유 파일 수정 (위 7번 목록) — PR에 요청으로만
- `main` 브랜치에서 직접 작업 — 항상 브랜치를 새로 판다
- 실존 인물 정보를 목업에 입력 — 수강생 데이터는 전원 가상 인물
- 교리 원문(교안·시리즈·어록) 요약·재작성 — 원문 그대로, 명백한 오탈자만
- `공유자료/` · `_archive/` 수정
- API 키 커밋 — `.env`는 git에서 제외돼 있고, 실수로 올렸으면 키를 폐기·재발급한다

## 자주 걸리는 것

- **내 대시보드에서 등록한 자료가 동료에게 안 보인다** — 정상이다. localStorage는
  각자 브라우저별이다. 다 같이 봐야 하는 데이터는 이슈로 리드에게 시드 등록을 요청한다
- **push가 거부된다** — 6번을 건너뛴 경우가 대부분. `git pull origin main` 후 재시도
- **화면이 하얗게 빈다** — 파일명에 `~` `[` `]` `#`이 들어가면 dev 서버가 403을 낸다
