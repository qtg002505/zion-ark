# 시온 아크(ZION ARK) 웹 프로토타입 — AI 세션 작업 지침

이 저장소는 시온 아크 플랫폼의 **웹 프런트엔드 프로토타입**이다 (React 19 + Vite + TS + Tailwind 4).
원 운영 저장소(vinext + Cloudflare Workers/D1)와 별개이며, 화면·권한·동선을 먼저 완성해
검증하는 용도다. 단, **데이터 계약·권한 모델·불변식은 원 저장소와 동일하게 유지한다** —
백엔드 연동 시 그대로 이식하기 위해서다.

기획 원문: `C:\Users\user\Desktop\시온 아크 프로젝트\` (1단계-착수지시문 · 현재-상태 · 권한-결정사항)
및 `zion-ark-dev` 스킬.

## 응답 어투 (2026-08-06 지적 — 예외 없음)

사용자에게 답할 때는 **항상 존댓말**을 쓴다. 반말은 쓰지 않는다.
"~한다 / ~했다" 서술체도 사용자에게 말을 거는 문장에서는 반말로 읽히므로
"~합니다 / ~했습니다 / ~해 주세요"로 쓴다. 간결하게 쓰라는 지시가 있어도 어투는 유지한다.

코드 주석 · 커밋 메시지 · 이 저장소의 문서(`docs/**`, `CLAUDE.md`)는 종전 서술체를 그대로
쓴다 — 사람에게 말을 거는 글이 아니라 기록이기 때문이다.

## 세션 시작

1. **`docs/HANDOFF.md` 하나만 먼저 읽는다** — 지금 상태·다음 할 일·막힌 것이 다 있다
2. `git log --oneline -5` / `git status --short`
3. 개발 셸은 Windows PowerShell: `npm.cmd`, `npx.cmd`

⚠️ **문서를 통째로 여러 개 읽지 않는다.** `docs/HISTORY.md`(지난 기록)는 "왜 이렇게 됐나"를
찾을 때만, 다른 문서는 그 주제를 다룰 때만 연다. 큰 파일은 `Grep`으로 필요한 대목만 본다.

작업 절차와 함정(원문 이관·화면 추가·색 변경·프리뷰 배포·검증 요령)은 `zion-ark-web`
스킬에 있다 — 이 저장소에서 작업할 때 자동으로 적용된다.

## 검증 (핸드오프 전)

```
npx.cmd tsc -b --noEmit
npm.cmd run build
```

화면을 건드렸으면 여기에 더한다 — 이 셋은 실제로 결함을 잡아낸 항목이다.

- 미리보기에서 직접 열어 확인. 콘솔 오류 0
- **모바일 폭(375)에서 가로 넘침 0**:
  `document.body.scrollWidth - document.documentElement.clientWidth > 0` 이면 넘친 것이다.
  ⚠️ `===`로 견주지 않는다 — `scrollbar-gutter: stable` 때문에 **본문이 뷰포트보다 좁을 때**가
  있어(내용이 짧아 세로 스크롤이 없는 화면) 멀쩡한 화면이 넘침으로 잡힌다
- **외부 사이트 임베드 여부는 개발용 인앱 브라우저를 믿지 않는다** — 실제 브라우저에서 확인한다
  (인앱 브라우저는 `X-Frame-Options`를 무시해 "표시된다"고 잘못 판단한 적이 있다)

## 데이터 계약 — 임의 변경 금지

- **역할 코드** (`src/lib/types.ts`): `headquarters_admin` · `tribe_admin` · `church_admin` ·
  `instructor` · `evangelist` · `content_admin` · `security_auditor`
- **조직 범위**: `national` · `tribe` · `church` · `cohort` · `division`
- **자료실 카테고리**: `standard_lecture` · `class_material` · `excellent_plan` + `isFeatured`
- **공지·어록 kind**: `notice_hq` · `notice_tribe` · `quote` · `video` (workspace_entries 계약)
- **출결 어휘**: `미입력`→unknown · `결석`→absent · `금주보강`→makeupPending ·
  `추후완료`→makeupDone · `대면` 포함→present
- 어휘: **사명자** = 강사+전도사. **강사·전도사 모두 담당 기수 전체**를 본다
  (2026-08-06 변경 — 전도사 `division` → `cohort`. `docs/decisions/ORG_CHART.md`)
  - ⚠️ 조직도상 "총회 사명자"(총회 관리직 전체)는 이 어휘와 **다른 용법**이다. 화면 문구에서 섞지 않는다

## 권한 확정값 (2026-08-05 리드 — 재질문 금지)

- 총회 공지 작성: `headquarters_admin` + `content_admin` / 지파 공지: 해당 지파 `tribe_admin`
- 어록·영상·자료실 등록: `content_admin` + `headquarters_admin`
- 우수 교안 지정: **`tribe_admin`(지파 신학부장)**, 승인 워크플로우 없음
  (2026-08-15 리드 지시 — 종전 「`headquarters_admin`만」을 대체한다.
  ⚠️ 「지파 공유 승격」과 주체가 겹친다 — 합칠지는 리드 확인 대기)
- 열람은 로그인 전체. 본부·지파의 강사/전도사 도우미 사용은 "보기만"
- 시범 로그인 당분간 유지 — **실제 개인정보 투입 전 반드시 정리**

## 조직·권한 (2026-08-06 확정 — `docs/decisions/ORG_CHART.md`)

- 계층은 **총회 → 지파 → 교회 → 기수** 4단계
- **관리직**(신학부장 이상)은 담당 기수가 없다 → 하위 조직 전체 집계를 본다
- **실무직**(강사·전도사)만 기수에 배정되고, **둘 다 담당 기수 전체**를 본다
- **분반은 권한 경계가 아니다.** `division` 스코프와 `divisions` 데이터는 스키마에 남기되
  (지우면 후방 마이그레이션 — 불변식 10) **권한 판정에는 쓰지 않는다**
- 착지 화면: 관리직 → 전체현황(요약) · 실무직 → 기수현황. 편의일 뿐 권한이 아니다
- **"총회 사명자"** = 총회 신학부장 · 총회 행정서무 · 총회 신학과장 → 기존 `headquarters_admin`에
  대응한다. 새 역할 코드를 만들지 않는다
  (⚠️ 조직도 표기는 "총회 신학서무", 리드 답변은 "총회 행정서무" — 표기 통일은 미확인)

## 기수 기록 권한 (2026-08-06 확정 — 재질문 금지)

- **월간·주간 계획**(`/plan` — 2026-08-13에 「기수 주간계획」에서 이름 변경)과
  **주차 사유·극복**(기수현황 「주간 흐름」)은
  **해당 기수의 강사·전도사만** 고친다 (`canEditCohortRecord`). 열람은 담당 범위 안에서 누구나
- 여럿이 같은 글을 고치므로 **주간계획은 수정 이력을 남긴다** (최근 20건)
- **상담 사례**(`/cases`) 익명화 기준: **지파 · 교회 · 센터(기수)까지만.**
  그 아래(이름·연락처·분반·나이)는 적지 않으며, `scanPII`가 걸리면 제출을 막는다.
  ⚠️ 정규식은 실수를 잡는 장치일 뿐 — **서버 연동 시 서버에서도 같은 검사를 한다**

## 자료실 구조 (2026-08-13 리드 지시로 개편)

- 자료실은 **독립 대메뉴**이고 **폴더 한 줄기**다. **구획(`LibrarySection`)은 폐지됐다** —
  종전 `instructor`(강사 도우미 자료실) · `external`(외부 자료실) 둘로 갈렸던 것을 없앴다
  - **「외부 자료실」이라는 것은 없었다.** 외부 매체(비메오·위플)는 **자료에 붙는 참고 링크**
    (`videoUrl`·`externalUrl`)를 뜻한 것이지 보관 구획이 아니다
  - ⚠️ **타입·필드(`section`)는 지우지 않는다.** 저장된 자료가 값을 갖고 있어 지우면 후방
    마이그레이션이 된다(불변식 10) — `division`과 같은 취급으로 **읽지 않고 남긴다**
- 자료실이 지금 품는 것: **아카이브**(실상 뮤지컬 영상 · 계시록 삽화 — **여기서 내려받아
  확인한다**) · **말씀광장** · **천지일보** · 신천지도서(시리즈) · 자료실 전체 보기
  - 「지파별 취합 도서」는 2026-08-13에 뺐다
  - 말씀광장과 천지일보는 **따로 선 묶음**이다 (종전 「외부 매체」 하나로 묶었던 것을 갈랐다)
- **모든 자료는 로그인해야 열린다. 무세션 401 원칙에 예외를 만들지 않는다**
- **밭갈이 네 파트와 예배설교는 강의 도우미 안에서 연다** (`/teaching`) — 초·중·고처럼 제
  카테고리 안에서 끝난다. 자료실로 나가지 않는다
- 자료 목록·상세·등록 몸통은 **`src/components/FolderLibrary.tsx` 한 벌**이다.
  자료실(`Library`)과 강의 도우미(`TeachingLibrary`)가 props로만 갈린다 — **복제 금지**
- 폴더는 `LIBRARY_FOLDERS`(`src/lib/types.ts`) 한 곳에서만 정의한다. 내비는 여기서 읽어 만든다
- `folderPath: string[]` — 지금은 1단계만 쓰지만 더 깊어져도 구조가 안 바뀐다.
  실연동 시 `library_materials.path`에 대응
- **용어 정의는 2026-08-08에 전부 받았다.** 원문은 `src/content/glossary.ts`,
  연결 관계와 남은 확인 사항은 `docs/decisions/GLOSSARY.md`.
  ⚠️ **정의가 왔어도 코드 값(enum·DB 코드)으로는 굳히지 않는다** — 상담 테마는 번호로,
  자료실 폴더는 문자열로 그대로 둔다. 굳혀서 얻는 것이 없고 표현이 다듬어지면
  후방 마이그레이션이 된다(불변식 10). 정의문은 **원문 그대로 싣고 요약·재작성하지 않는다**

## 불변식

1. 수강생 조회는 담당 배정 범위로 스코프 — **최소 단위는 기수**. (서버 연동 시 401/403 계약)
2. 원문 개인정보는 외부 링크·아티팩트·스크린샷·로그 금지 — 집계·통계만 반출
3. 출결 원본은 읽기 전용 — 되쓰기 UI 만들지 않음
4. AI는 수강생의 신앙·인격·심리를 확정 판정하지 않음. 답변에 출처 표시.
   **외부 AI에는 동의받고 비식별화한 정보만 보낸다** (2026-08-11 리드 변경 —
   종전 「수강생 개인정보를 AI 입력에 넣지 않음」을 이 문장이 대체한다).
   덕분에 외부 AI·텔레그램 봇 연동이 열렸고, 대신 **보내기 전에 셋을 지킨다**:
   - **동의**: 동의가 확인된 기수·수강생의 기록만 보낸다. 동의 여부를 모르면 보내지 않는다
   - **비식별화**: `src/lib/privacy.ts`의 `redactForAI()`를 **반드시 거친다.**
     이름·연락처·생년월일·나이·분반은 나가지 않는다. 소속은 지파·교회·기수까지만
   - **두 번 검사**: 브라우저에서 한 번, **서버에서 다시** 한다.
     ⚠️ 정규식은 실수를 잡는 장치일 뿐 완전한 차단이 아니다 — 사람이 쓴 이름은 못 잡는다
   불변식 2(원문 개인정보 반출 금지)는 그대로다 — 비식별화를 거치지 않은 원문은 여전히 못 나간다
   - 관찰문을 규칙 엔진으로 읽어 외부 GPT 프롬프트로 잇던 **「사이트 안 1차 조언」은
     2026-08-13에 만들고 같은 날 없앴다** (`/tendency` 한 곳뿐이던 `advice-engine.ts` ·
     `AdvicePanel.tsx` · `advice-corpus.ts`를 함께 지웠다 — 되살릴 때는 git 이력에서 꺼낸다).
     `/tendency`는 이제 **기록된 `enneagramType`의 원문을 그대로** 보여 줄 뿐이다
     (`/enneagram`과 같은 소스·같은 렌더러). 관찰문으로 유형을 추정하지 않는다는 원칙,
     사이트가 새 문장을 만들지 않는다는 원칙(불변식 5)은 그대로 유효하다 — 규칙 기반
     조언을 다시 붙일 때는 이 방식을 따른다
5. 교리 콘텐츠(교안·시리즈 본문)는 원문 그대로 이관 — **재작성 금지**
   - **사이트가 「고른 것」은 원문과 갈라 표시한다** (2026-08-13, 강의 녹취 정리에서 처음 적용).
     원문에서 자르기만 한 것과 사이트가 골라낸 것을 **화면에서 묶음부터 나눈다**
     (`① 원문에서 뽑은 것` / `② 사이트가 고른 문장`).
     고른 것에는 **왜 골랐는지를 타입이 필수로 강제**한다 (`DigestPick.why`) —
     이유 없는 선택을 만들 수 없게 해 두면 「요약」이 슬그머니 「재작성」이 되지 않는다
   - **매끄러운 요약문은 사이트가 쓰지 않는다.** 새 문장이 필요한 것(요약문·유머 찾기)은
     외부 AI 프롬프트로 넘긴다
6. 목업 데이터는 반드시 가상 인물임을 화면에 표기

## 화면 규칙 (2026-08-05 확정)

- **팔레트는 `src/index.css`의 `@theme`에서만 고친다.** 화면 파일에 색을 하드코딩하지 않는다
  (`zion-*` **네이비 주색**(2026-08-10 녹색에서 변경) · `gold-*` 우수 교안·NEW 뱃지 ·
  `surface`/`ink`/`ink-soft`). 시안 원본은 `Design/`(참조용, git 제외)
  - 이 규칙 덕에 2026-08-10 색 전환이 **`@theme` 한 곳 수정으로 끝났다** — 화면 파일은
    한 곳도 안 고쳤고 남은 녹색 잔재 0을 실측으로 확인했다
  - 색을 바꾸면 **대비를 실측한다.** 캔버스에 칠해 픽셀을 읽는다 — `getComputedStyle`은
    `oklch` 문자열을 그대로 돌려줘 숫자를 rgb로 잘못 읽는다. 본문·버튼·뱃지 조합이
    WCAG AA(4.5) 이상인지 본다 (2026-08-10에 `gold-700`이 4.47로 미달해 조정했다)
- **다크 모드도 `index.css` 한 곳에서 끝낸다** (2026-08-11). 화면 파일에 `dark:`를
  뿌리지 않는다 — `[data-theme="dark"]`가 팔레트 변수를 갈아 끼우면 전 화면이 따라온다
  - ⚠️ **진한 배경은 정해진 클래스만 쓴다** — `bg-white`(`/95` 포함) · `bg-zion-600/700/800/900` ·
    `border-zion-700/800` · `bg-gold-500`. 이들만 `index.css`의 「진한 면」 블록에서
    되돌려 놓았다. **목록 밖 단계를 배경으로 쓰면 다크에서 하얗게 뜬다**
    (변수는 글자 쪽에 맞춰 뒤집혀 있다 — `text-zion-700`이 밝아야 하기 때문이다)
    - **반투명도 따로 적어야 한다.** `.bg-white`를 되돌려도 `.bg-white/95`는 안 걸린다 —
      수강생 상세 팝업 머리가 어두운 화면에서 흰 띠로 떠 있었다(2026-08-11에 막았다)
- **밝은·어두운 두 화면 모두 AA(4.5)를 통과한 상태다** (2026-08-11 실측).
  ⚠️ `zion-300`처럼 **한 단계가 글자와 옅은 면을 겸하면 변수를 낮추지 않는다** —
  `border-zion-300`이 굵고 어두워지고 `disabled:bg-zion-300` 버튼이 눌리는 것처럼 보인다.
  `.text-zion-300`만 `--text-zion-muted`로 되돌려 뒀고, 이런 되돌리기는 **`@layer utilities`
  안에** 둔다 (레이어 밖에 두면 `hover:text-*` 같은 변형까지 이겨 버린다)
  - `zion-950`과 `gold-500`은 뒤집지 않는다. NEW 뱃지(`bg-gold-500 text-zion-950`)와
    드로어 덮개(`bg-zion-950/40`)가 이 값에 기대고 있다
- **화면 위에 띄우는 것은 `src/components/Portal`로 감싼다** (2026-08-13) —
  모달 · 팝오버 · 시트 전부. `fixed inset-0`에 `z-50`을 줘도 **`main` 안에 있으면 헤더에 눌린다**
  (`main`이 `view-transition-name` 때문에 쌓임 맥락을 만든다). 숫자를 올려도 안 고쳐진다 —
  맥락 밖으로 꺼내야 한다. `content-visibility`·`transform`이 `fixed`를 가두는 문제도 함께 풀린다
- **화면 링크는 `src/components/TransitionLink`에서 가져온다** (2026-08-11).
  `react-router-dom`의 `Link`·`NavLink`를 직접 쓰면 **화면 전환 효과가 조용히 빠진다** —
  라우터의 `viewTransition` prop은 데이터 라우터에서만 도는데 이 앱은 `<BrowserRouter>`다
- **화면 문구 규칙** (2026-08-16 전면 점검으로 확정 — AI 냄새를 걷어냈다):
  - 본문 문구에 **이모지를 쓰지 않는다** (⚠️ 포함 — 채팅 문서처럼 보인다). 주의 표시가
    필요하면 「유의」 같은 글자 라벨을 쓴다. 코드 주석의 ⚠️는 그대로 둔다
  - **챗봇식 권유를 쓰지 않는다** — 「~해 보세요」·「물어보세요」·「필요하신가요?」 대신
    무엇이 어떻게 되는지를 적는다 (「테마를 누르면 펼쳐집니다」)
  - **외래어 명명을 피한다** — 「액션」→「활동」. 검색창 안내는 질문 유도가 아니라
    **예시를 든 찾기 안내**로 쓴다 (「주제로 찾기 (예: 전도, 믿음)」)
  - 화면 아래 안내문이 길어지면 **문단을 끊는다** — 줄표(—)로 이어 붙인 한 문단은 안 읽힌다
  - 확인 버튼은 **「저장」**으로 통일한다 (「고침」 같은 새 말을 만들지 않는다).
    빈 화면 안내는 「무엇이 없고, 어디를 누르면 되는지」 두 가지만 적는다
- **긴 자료는 소주제 단위로 접는다** (`src/components/Accordion.tsx`). 통글로 쏟아내지 않는다
  - 소주제가 **수십 개인 화면에서만** `deferOffscreen`을 켠다 (지금은 어록 하나).
    적은 화면에 켜면 얻는 것 없이 스크롤 길이만 어긋난다.
    ⚠️ **소주제 안에 모달이 있으면 켜지 않는다** — `contain: paint` 때문에 화면 가운데
    떠야 할 창이 그 항목 안에 갇힌다
- **내비는 3단**(대주제 → 하위 묶음 → 항목, `src/shell/nav.ts`).
  대주제는 한 번에 하나만 열리고, 하위 묶음은 여럿 열려 있어도 된다.
  하위가 없는 대주제는 `to`만 주면 바로 이동한다.
  **하위 묶음이 직속 항목보다 먼저 그려진다** — 직속 항목은 보조 도구라 뒤에 둔다
- **카테고리 순서는 2026-08-09 기준** — 임의로 바꾸지 않는다 (`src/shell/nav.ts`가 정본):
  홈·전체 현황 / 기수 현황 · 강의 도우미 · 분반·보강 도우미 · 수강생 관리 도우미 ·
  상담 도우미 · 자료실 · **공지·어록** · 사명자 심방 도우미
  - **12지파 선교센터**는 2026-08-11에 **맨 뒤에** 더한 대주제다 — 기존 순서는 건드리지 않았다
  - 2026-08-13에 **「홈」과 「전체 현황」을 갈랐고**, 「관찰 필요」·「사명자 심방 도우미」를 없앴다
    (경로는 리다이렉트로 남긴다. `lib/attendance-signals.ts`는 등급 판정이 쓰므로 그대로 둔다)
  - 강의 도우미 = 밭갈이 → 초등 → 중등 → 고등 → **예배설교** · 분반·보강 = 분반 자료 →
    보강 자료 → 보강 콘텐츠 (2026-08-13에 예배설교를 직속 항목에서 하위 묶음으로 올렸다)
  - 자료실 = 신천지도서(시리즈) → 아카이브 → **말씀광장** → **천지일보** + 전체 보기 하나
    (2026-08-13 — 종전 「외부 매체」 한 묶음과 「구획 전체 보기 둘」을 대체한다)
  - 수강생 관리 도우미 = 수강생 현황 → 관찰 필요 → **수강생 성향 분석** → 성향 참고
  - **공지·어록은 자료실 밖 독립 대주제다** (2026-08-09 리드 지시 — 2026-08-08 개편에서
    자료실 안에 넣었던 것을 되돌렸다). 저장 구조는 `workspace_entries` 그대로다
- **폴더 목록은 사이드바(`nav.ts`)에만 둔다** (2026-08-09 리드 지시).
  화면이 자체 폴더 패널을 그리면 **같은 카테고리가 두 군데에 뜬다** — 자료실 화면에서
  실제로 그랬고 걷어냈다. 화면은 고른 폴더의 자료만 보여 준다
- **좁은 화면을 1급으로 다룬다** — 현장에서 휴대전화로 자료를 연다.
  `lg` 미만은 사이드바가 드로어, 2단 구성은 1단으로 쌓임
- **전체현황은 점검자용 요약이다** (2026-08-06 변경). 출석률 분포·대면 시간대 같은 상세 분석은
  기수현황에 둔다 — 한 기수를 파고드는 도구여서 전국·지파 요약 화면에는 맞지 않는다.
  (종전 지침 "분포는 전체현황이 가장 먼저 드러내야 할 사실"을 이 결정이 대체한다)
  - **메인(`/`)과 전체현황(`/overview`)은 다른 화면이다** (2026-08-13 리드 지시로 분리).
    메인은 **카테고리를 고르는 홈페이지**(로고를 누르면 온다), 전체현황은 점검자용 수치 요약이다.
    카테고리 타일은 `visibleNavGroups`에서 **파생**한다 — 메인에 목록을 다시 적으면 nav.ts와 어긋난다
- **모든 검색은 띄어쓰기를 무시한다** (`src/lib/text-match.ts`, 2026-08-13 리드 지시).
  새 검색창을 만들면 `looseIncludes`를 쓴다 — `String.includes`를 직접 쓰지 않는다.
  ⚠️ 스니펫 자리는 `looseIndexOf`로 **원문에서** 찾는다(공백 제거 문자열의 위치는 어긋난다)
## 과수(진도) 제목 — 2026-08-15 리드 전달 · 정본

**진도 표기는 `src/content/curriculum-titles.ts` 하나가 정본이다.** 교안 원문 파일명·강 제목에서
뽑아 쓰지 않는다 — 그렇게 하다 화면마다 표기가 달라졌다.

- **초등 25과수** · **중등 25과수** · **고등 23과수**(계 1~22장)
- ⚠️ **문구를 다듬지 않는다**(불변식 5). 「세가지 이스라엘」의 띄어쓰기도 원문 그대로다.
  고칠 일이 있으면 리드에게 확인받고 그 파일만 고친다
- **회차와 강은 1:1이 아니다.** 리드가 함께 준 운영 규칙 때문이다:
  - **초등**: 한 과수를 **나눠서** 진행할 수 있다 — 기수 재량 (예: 「산(1)」·「산(2)」)
  - **중등**: 목록 중 **몇 개를 뽑아** 진행한다 (전부 하지 않는다)
  - **고등**: 계 1~22장을 **전부** 진행한다
  - **특강**: 중간중간 강사가 제목을 지어 진행한다 (출석률에 안 들어간다 — 「특강」 항목 참고)
- 그래서 「N회차 = 무슨 강」은 **기수마다 다르다.** `curriculum-mock.ts`의 `CURRICULUM`은
  그것을 순서대로 편 **시범 값**이고, 남는 회차는 `undecided`(화면에 「미정」)로 비워 둔다 —
  어느 과수를 나눌지·무엇을 뽑을지는 지어내지 않는다(불변식 6)
- **단계 색**: 초등 하늘색 · 중등 주황색 · 고등 남색.
  값은 `index.css`의 `@theme`(`--color-level-*`)에 있고 화면은 `LEVEL_TONE`만 가져다 쓴다
- ⛔ **연속 번호 표기 금지 — 학원법 때문이다** (2026-08-15 리드 지시).
  「초등1 · 초등2 · 초등3」·「초1강」·「중12강」처럼 **단계에 연속 번호를 붙인 표기를 쓰지 않는다** —
  학년·학기 편성처럼 읽힌다. 화면에는 **단계(초·중·고) + 과수 제목**만 낸다.
  - 번호(`lessonNo`)는 **데이터로는 남긴다** — 차례를 매기고 회차와 짝짓는 데 쓴다. 화면에만 안 낸다
  - **회차(`N회차`)는 쓴다** — 출결을 세는 단위이고 과정 편성 표기가 아니다.
    ⚠️ 회차를 「N강」으로 적지 않는다 (주간계획에서 실제로 그랬고 2026-08-15에 고쳤다)
  - 개수 표기(「초등 25과수」·「고등 23강」)는 연속 구분이 아니라 세는 말이라 괜찮다

- **기수 일정 산수는 `src/lib/cohort-calendar.ts` 한 곳이다** (2026-08-13 — 8개월 확정).
  수업 요일은 **기수 도중에 바뀐다** — 개강~6개월차 월·화·목, 6~8개월차 **일·수·목**
  (2026-08-15 리드 확정: 월요일 수업이 일요일로, 화요일 수업이 수요일로 옮겨진다).
  요일을 읽는 곳은 전부 `weekdaysOfWeek(주차, 구간)`을 거친다 — `CLASS_WEEKDAYS`를 직접 보지 않는다.
  ⚠️ **일요일은 그 주의 첫날(월−1)이다.** `offsetInWeek`과 `weekNoOf` **두 곳이 같은 규칙**을
  써야 한다 — 어긋나면 구간 첫 주의 일요일 수업이 앞 주차로 밀려 회차에서 빠진다.
  ⚠️ **주차 라벨(「6월 2주」)은 그 주 목요일 기준** — 저장된 주차 기록이 이 라벨로 조인되므로
  규칙을 바꾸면 기록이 끊어진다. 일정 표시값은 `effectiveSchedule()`(화면 수정 병합)을 거친다
- **자료 목록은 게시판 표다** (2026-08-13 리드 확정 — `FolderLibrary` 한 곳).
  추천은 `helpfulBy` 1인 1표 토글, 조회수는 상세를 **여는 클릭에서만** 센다.
  ⚠️ 표 행을 버튼으로 만들지 않는다 — 행 안 즐겨찾기 버튼과 중첩이 된다
- **외부 매체(말씀광장·천지일보)는 새 탭으로 연다.** 두 매체가 외부 표시를 막고 있어
  사이트 안에 띄울 수 없다 — 백엔드 프록시가 조건이며 착수 방법은 `docs/HANDOFF.md`

## 교체 경계 (백엔드 연동 시 이 파일들만 바뀐다)

- `src/lib/auth.tsx` — 시범 로그인 → 휴대전화 인증 / 상위 대시보드 SSO
- `src/lib/store.tsx` — localStorage → API 클라이언트 (library-materials · workspace-entries)
- `src/content/cohort-mock.ts` — 목업 → attendance-adapter (읽기 전용 시트 → 표준 모델)
- `src/lib/search.ts` — 로컬 검색 → AI API (검색 대상은 공통 교육 영역 유지)

화면 컴포넌트(`src/pages/**`)는 이 경계 뒤의 구현에 의존하지 않게 유지한다.

## 팀 파트 경계 (5인 분업 시)

사용자가 파트 담당 팀원이라고 밝히면 `docs/TEAMWORK.md` 분담표의 **그 파트 파일만** 고친다.
공유 파일(`types.ts` · `store.tsx` · `nav.ts` · `App.tsx` · `permissions.ts` · `index.css` ·
`shell/**` · `docs/**` · 이 파일)은 고치지 않는다 — 변경이 필요하면 코드 대신
**"공유 파일 변경 요청" 목록**으로 정리해 PR 본문에 넣게 한다. 리드가 반영한다.

## 하지 말 것

- 스펙 없는 기능 임의 추가 (기획 문서·착수지시문 범위 밖)
- 데이터 계약(역할 코드·카테고리·kind·출결 어휘) 변경
- 교리 내용 재작성
- 실존 인물 정보를 목업에 사용
- `_archive/` 수정 (이전 프로젝트 보관용)

## 문서 갱신

작업 후 `docs/HANDOFF.md` 갱신. 화면·권한 구조가 바뀌면 `docs/ARCHITECTURE.md`도.
원 기획 폴더(`시온 아크 프로젝트`)의 `현재-상태.md`는 리드가 관리 — 직접 수정하지 않는다.
